services:
  perplexica:
    image: itzcrazykns1337/perplexica@sha256:7e11eed65bdcf6f4adc430a21fc3f904468f78e7ec69a861bef5e9da293cf552
    container_name: dream-perplexica
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    environment:
      - SEARXNG_API_URL=http://searxng:8080
      - OPENAI_BASE_URL=${LLM_API_URL:-http://llama-server:8080}/v1
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
    volumes:
      - perplexica-data:/home/perplexica/data
      - perplexica-uploads:/home/perplexica/uploads
    ports:
      - "${PERPLEXICA_PORT:-3004}:3000"
    depends_on:
      searxng:
        condition: service_healthy
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 2G
        reservations:
          cpus: '0.25'
          memory: 256M
    healthcheck:
      test: ["CMD", "node", "-e", "const h=require('http');h.get('http://'+require('os').hostname()+':3000/',r=>{process.exit(r.statusCode===200?0:1)}).on('error',()=>process.exit(1))"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

volumes:
  perplexica-data:
  perplexica-uploads:
