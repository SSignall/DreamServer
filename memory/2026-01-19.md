# 2026-01-19 — Extension Fixes & New Issues

## Commits Pushed (DreamServer Public Repo)
- Frigate NVR port fix: `8971:5000` mapping corrected
- RVC workflow: Path traversal fix, URL syntax fix, deprecated node updated
- Whisper workflow: Binary handling fix
- TTS/Frigate integration workflows: Deprecated `functions` → `code` node

## Todd Feedback (2026-03-07)
- InvokeAI v6→v5 downgrade was **intentional** — v6.0.0 broke model format, rollback to v5.5.0 until migration tooling ready
- 415/415 extensions still passing validation
- Frigate port mismatch and n8n syntax issues flagged as **highest risk** for deploy

## Android-18 Review — Commit `bf79bc15` (Bark TTS Server)

**[CRITICAL]** Race condition in `_load_models()`: concurrent requests can trigger duplicate model initializations due to non-atomic check-then-act pattern on global state (server.py:25-30)

**[HIGH]** Missing input validation: `text` field accepts arbitrary length strings without limits, enabling DoS via memory exhaustion (server.py:60)

**[HIGH]** Information disclosure: Raw exception strings exposed to clients in HTTP 500 responses via `detail=str(e)` (server.py:76, 94)

**[MEDIUM]** Unvalidated `output_format` parameter passed directly to soundfile without whitelist validation (server.py:69)

**[MEDIUM]** Synchronous `generate_audio` blocks worker threads; CPU-intensive TTS should run in thread pool executor (server.py:64-65)

## Action Items
- Fix race condition with asyncio.Lock or threading.Lock
- Add text length limits (e.g., max 10K chars)
- Sanitize error messages — log full trace, return generic errors
- Whitelist output_format to supported formats (wav, mp3, ogg)
- Consider executor for CPU-intensive TTS
