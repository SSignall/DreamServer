# 2026-01-19 — Extension Fixes Complete

## Summary
Fixed all CRITICAL and HIGH severity issues flagged by Android-18 in commits `1cf1dee8`, `3101698c`, and `bf79bc15`.

## Commits Pushed to dev/main

### 1. Frigate NVR — Port Mismatch (CRITICAL)
**File:** `extensions/services/frigate-nvr/docker-compose.yml`
**Commit:** part of `4560e9c3` batch
**Fix:** `8971:5000` — Frigate internally listens on 5000, not 8971

### 2. n8n Workflow Templates
**Files:** 
- `extensions/n8n/workflow-templates/rvc-tts-workflow.json`
- `extensions/n8n/workflow-templates/whisper-workflow.json`
- `extensions/n8n/workflow-templates/tts-integration-workflow.json`
- `extensions/n8n/workflow-templates/frigate-integration-workflow.json`

**Fixes:**
- Path traversal vulnerability sanitized (`model` parameter strips `../`)
- URL syntax: JS template literal → n8n string concatenation
- Deprecated `n8n-nodes-base.functions` → `n8n-nodes-base.code`
- Binary file handling for multipart uploads (Whisper)

### 3. Bark Server — Security Hardening
**File:** `extensions/services/bark/server.py`
**Commit:** `cb522e7e`

**[CRITICAL]** Race condition in `_load_models()` → double-checked locking with `threading.Lock()`
**[HIGH]** DoS via memory exhaustion → `max_length=10000` validation on text field
**[HIGH]** Info disclosure → log full exceptions, return generic error messages to clients
**[MEDIUM]** Unvalidated `output_format` → whitelist (WAV, MP3, OGG, FLAC)
**[MEDIUM]** Worker starvation → `ThreadPoolExecutor` for CPU-intensive TTS

**Version bumped:** 0.1.5 → 0.1.6

## Context from Todd
- InvokeAI v6→v5 downgrade was **intentional** — v6.0.0 broke model format, rollback until migration tooling ready
- 415/415 extensions still passing validation
- Frigate port mismatch and n8n syntax issues flagged as **highest risk** for deploy

## Status
- All CRITICAL issues resolved
- All HIGH severity issues resolved
- Pushed to `dev/main` (Lighthouse-AI-Dev repo)
- Ready for Android-18 re-review
