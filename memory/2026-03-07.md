# 2026-03-07 — Android-17 Session Log

## QA Review: Wave 2 Extensions + Hardening

Reviewed Android-16's recent commits:
- `5176baa1` — ComfyUI: All git clones pinned to specific SHAs, `seccomp:unconfined` and `SYS_PTRACE` removed from AMD compose ✓
- `ee2cb0ca` — Forge: Image pinned to SHA256 digest ✓
- `1dab00e6` — AudioCraft: GPU reservation added ✓
- `cf4c31f3` — InvokeAI: Bumped to v6.11.1 (latest stable) ✓

**Pre-existing issues identified:**
- `aider/`, `piper-audio/` — missing health fields, healthchecks, security_opt
- `chromadb/`, `immich/`, `jupyter/`, `rvc/` — missing security_opt
- `milvus/` — empty directory (needs cleanup or completion)
- `xtts/` — still using `:latest-cuda121` floating tag

## Critical Fix: Extension Path Structure

**Problem discovered:** All extension work was being committed to `dream-server/extensions/` instead of root `extensions/`. The build system looks at root `extensions/`, making all work undiscoverable.

**Fix applied:**
- Moved all 44 extensions from `dream-server/extensions/services/` to `extensions/services/`
- Moved schema, templates, workflows to root `extensions/`
- Commit: `1fc9f2cc` — 203 files changed, +23,985 insertions
- Pushed to dev remote

**Guidance sent to Android-16:** Commit extensions to `extensions/services/<name>/` NOT `dream-server/extensions/`

## Wave 3 Status

Android-16 shipped 5 new extensions in batch:
- Jan (#21) — Local LLM ChatGPT alternative
- Label Studio (#25) — Data labeling for ML
- XTTS (#28) — Multilingual TTS with voice cloning
- CrewAI (#29) — Multi-agent workflow builder
- Continue (#30) — AI coding assistant
- Plus Milvus (#24) — Vector database

All now in correct location after path fix.

## Workstream Division

Confirmed with Android-18: 16 builds, 17 reviews. My frontier reasoning (K2.5) catches what 16's 80B model misses. When review queue clears, I pick up hardening items or upstream monitoring.

## Blockers

**InvokeAI Docker Image Issue** — Committed `cf4c31f3` + `2a11f566`
- Neither `v5.5.0` nor `v6.11.1` tags exist on GHCR (`ghcr.io/invoke-ai/invokeai`)
- Official project uses **Launcher-based installation**, not Docker
- Recommendation: Remove InvokeAI extension until Docker image source confirmed

**Status**: Flagged for Michael input. 16 is unblocked on other items.
