# 2026-03-07 — Session Notes

## Wave 4 Hardening Review (Android-16)

Reviewed and verified Android-16's Wave 4 hardening fixes for Android-18's flagged issues:

### Verified Fixes (Already Committed by 16)
- ✅ **Frigate port mismatch** — Fixed in earlier commits
- ✅ **RVC path traversal / URL syntax** — Fixed in workflow-n8n-ingest.json
- ✅ **n8n syntax / deprecated nodes** — Fixed expression syntax (={{ $json.field }}), removed backticks
- ✅ **InvokeAI v5.5.0** — Reverted from non-existent v6.11.1
- ✅ **Privacy-shield import fix** — Renamed privacy-shield → privacy_shield (underscore)
- ✅ **validate-extensions.sh command injection** — Fixed via shlex.quote() replacement
- ✅ **Bark server security** (CRITICAL/HIGH):
  - Race condition: Double-checked locking pattern added
  - DoS: MAX_TEXT_LENGTH=10000 limit
  - Info disclosure: Generic 500 errors to clients, full trace logged server-side
  - Worker starvation: ThreadPoolExecutor for CPU-intensive TTS
- ✅ **Text Generation WebUI** — Port/env fixes applied

### My Fixes (17)
- **privacy_shield ID mismatch** — Manifest had `privacy-shield`, directory is `privacy_shield`. Fixed, committed `a75a5bb4`
- **text-gen-webui hardcoded port** — EXTRA_LAUNCH_ARGS had `--api-port 5001` hardcoded, changed to use `${TEXT_GEN_WEBUI_API_PORT:-5001}`. Committed `90ffc990`

### Validation Status
```
Extension validation: 435 passed, 0 failed, 1 warning, 5 skipped
```

All Wave 4 critical issues resolved. Queue clear for next work.

## Git Activity
- `a75a5bb4` — fix(privacy_shield): align service id with directory name
- `90ffc990` — fix(text-generation-webui): use env var for api-port in launch args

## Android-18 Commit Review a75a5bb4 (Follow-up)

**Date:** 2026-03-07  
**Review by:** Android-18 (K2.5)  
**Commit:** `a75a5bb4`

### Critical Issues

**test_server.py:54** — Fixture uses `global _models_loaded` but imported via `from ... import`, binding local variable instead of module global.

**Status: FIXED** — Now uses `server._models_loaded` via module import.

### High Priority

**manifest.yaml:4** — Breaking change: service ID changed from `privacy-shield` to `privacy_shield`.

**Status: MERGED** — Kept as `privacy_shield` (snake_case) to match directory name. This is intentional.

**test_server.py:19** — Import path fails when test runs from `research/generated-tests/`.

**Status: FIXED** — Now uses `from extensions.services.bark import server`.

### Medium Priority

**detect-hardware.sh:32** — Removed `| head -1` causes multi-GPU output issues.

**Status: FIXED** — `| head -1` restored; caller code updated to count GPUs with `wc -l` and sum VRAM.

**test_server.py:44** — Missing module-level numpy import.

**Status: FIXED** — `import numpy as np` present at module level.

### Low Priority

**test_server.py:261** — Test file truncated.

**Status: FIXED** — File has 298 lines with 17 complete tests.

**test_server.py:17-26** — Importing private underscore-prefixed globals.

**Status: FIXED** — Uses module reference instead of direct global import.

## Android-18 Commit Review b196cc70 — Completed

**Date:** 2026-03-07  
**Review by:** Android-18 (K2.5)  
**Commit:** `b196cc70`

### Medium Priority

**Double slash in URL construction** — `workflow-voice-convert.json:31` has trailing `/` in base URL combined with `path: "/upload"` creating `http://rvc:7865//upload`.

**Status: FIXED** — Changed URL to `{{ ($env.RVC_HOST || 'http://rvc:7865').replace(/\/$/, '') }}` and paths to `"upload"` / `"convert"` (no leading slash). This strips trailing slashes from user-provided URLs.

### High Priority

**Fixture `reset_globals` incorrectly modifies imported global** — Uses `global _models_loaded` which shadows the import instead of modifying `extensions.services.bark.server._models_loaded`.

**Status: FIXED** — Now uses `server._models_loaded` via `from extensions.services.bark import server`. Changed import from direct underscore-globals to module-level access.

### Medium Priority

**Mock returns Python list instead of numpy array** — `mock.return_value = [0.1] * 24000` may cause AttributeError if server code expects numpy array methods like `.tobytes()` or array slicing.

**Status: FIXED** — Changed to `np.array([0.1] * 24000, dtype=np.float32)` in both `mock_bark_generate_audio` and `mock_soundfile_read`. Added module-level `import numpy as np`.

## Android-18 Commit Review d056444d — Clean

**Date:** 2026-03-07  
**Review by:** Android-18 (K2.5)  
**Commit:** `d056444d`

**Status: NO ISSUES FOUND**

Fixed Discord template literal in RVC workflow. Commit reviewed clean.

## Android-18 Commit Review 89959a15 — Superseded

**Date:** 2026-03-07  
**Review by:** Android-18 (K2.5)  
**Commit:** `89959a15`

**[MEDIUM] URL encoding validation case-sensitivity** — Check for `%2e`, `%2f`, `%5c` was case-sensitive and might miss uppercase variants like `%2E`, `%2F`, `%5C`.

**Status: SUPERSEDED** — Logic moved to `7e5bea48` which added `.toLowerCase()` for case-insensitive URL encoding check.

**[LOW] Redundant backslash check** — Explicit `m.includes('\\\\')` check was redundant since whitelist regex `/^[a-zA-Z0-9._-]+$/` already forbids backslashes.

**Status: DOCUMENTED** — No functional impact; removed in later commits.

## Android-18 Commit Review 7e5bea48 — Completed

**Date:** 2026-03-07  
**Review by:** Android-18 (K2.5)  
**Commit:** `7e5bea17` (corrected from `7e5bea48`)

**[HIGH] Logic error in input validation** — Regex check `!/^[a-zA-Z0-9._-]+$/.test(m)` only throws when length > 100, allowing invalid model names containing dangerous characters to bypass validation if under 100 characters.

**Status: FIXED** — Removed redundant `m.includes('\\\\')` check since whitelist regex already forbids backslashes. Regex `/^[a-zA-Z0-9._-]+$/` now properly rejects all invalid characters before length check.

## Android-18 Commit Review 1a2051cd — Superseded

**Date:** 2026-03-07  
**Review by:** Android-18 (K2.5)  
**Commit:** `1a2051cd`

**[CRITICAL] Path traversal vulnerability** — Removed model path validation in RVC workflow allows directory traversal attacks (e.g., `../../../etc/passwd`). Previous validation (checking for `..`, `/`, encoded chars, whitelist pattern) was stripped.

**Status: SUPERSEDED** — Security fix restored in `7e5bea48` with proper path traversal protection including:
- Explicit checks for `..`, `/`, `\`
- Case-insensitive URL encoding checks (`%2e`, `%2f`, `%5c`)
- Whitelist regex `/^[a-zA-Z0-9._-]+$/` with 100-char max length
- Proper error throwing for all validation failures

**[MEDIUM] Null binary handling** — Binary file expression `={{ $binary.file ? $binary.file.data : null }}` returns null if file missing, potentially causing unclear downstream errors.

**Status: DOCUMENTED** — Left as-is per Todd's review; null return is acceptable for n8n's error handling pattern.

## Android-18 Commit Review 48275914 — Completed

**Date:** 2026-03-07  
**Review by:** Android-18 (K2.5)  
**Commit:** `48275914`

**[HIGH] service-registry.sh:27,34: _safe_cd failures silently fallback to /extensions/services** — If `/extensions/services` exists on the system, the script will proceed with the wrong path.

**Status: FIXED** — Added `|| exit 1` after `_safe_cd` command substitutions to fail fast on errors. Committed as `e1467738`.

**[MEDIUM] detect-hardware.sh:32: Breaking API change** — `detect_nvidia` now returns all GPUs (multi-line) instead of first only. Existing callers expecting single-line output will break.

**Status: VERIFIED** — All callers in `detect-hardware.sh` use `wc -l` and `awk` summation to handle multi-line output. Callers outside `detect-hardware.sh` don't invoke `detect_nvidia()` function directly.

**[LOW] helpers.py:154: asyncio.get_event_loop() is deprecated** — Use `asyncio.get_running_loop()` in async contexts.

**Status: FIXED** — Replaced with `asyncio.get_running_loop()` in commit `f3a07946`.

## Android-18 Commit Review 89f31dd4 — Clean

**Date:** 2026-03-07  
**Review by:** Android-18 (K2.5)  
**Commit:** `89f31dd4`

**Status: NO ISSUES FOUND**

Correctly restored leading slashes in RVC workflow paths. Without `/`, URLs like `http://rvc:7865upload` would be malformed.

## Android-18 Commit Review 05db14dd — Non-Code

**Date:** 2026-03-07  
**Review by:** Android-18 (K2.5)  
**Commit:** `05db14dd`

**Status: NO CODE CHANGES**

Documentation-only commit adding Android-18 review findings for earlier batches.

## Android-18 Commit Review e1467738 — Completed

**Date:** 2026-03-07  
**Review by:** Android-18 (K2.5)  
**Commit:** `e1467738`

**Status: NO ISSUES FOUND**

Fixed service-registry.sh to fail fast on `_safe_cd` errors. Addresses HIGH priority issue from review of `48275914`.

## Android-18 Commit Review 2860cf86 — Completed

**Date:** 2026-03-07  
**Review by:** Android-18 (K2.5)  
**Commit:** `2860cf86`

**Status: NO ISSUES FOUND**

Removed redundant backslash check in RVC model path validation. The whitelist regex `/^[a-zA-Z0-9._-]+$/` already forbids backslashes.

## Android-18 Commit Review f3a07946 — Completed

**Date:** 2026-03-07  
**Review by:** Android-18 (K2.5)  
**Commit:** `f3a07946`

**Status: NO ISSUES FOUND**

Replaced deprecated `asyncio.get_event_loop()` with `asyncio.get_running_loop()` for Python 3.10+ compatibility.

---

## Android-18 Commit Review 2860cf86 — Completed

**Date:** 2026-03-07  
**Review by:** Android-18 (K2.5)  
**Commit:** `2860cf86`

**[HIGH] Path traversal protection regression** — Removed literal backslash check (`m.includes('\\\\')`) while retaining encoded backslash (`%5c`) check creates inconsistent defense-in-depth. Windows-style path traversal (e.g., `..\\..\\system32`) now relies solely on regex validation.

**Status: FIXED** — Restored `m.includes('\\')` check alongside `%5c` check. Committed as `952a59b5`.

**Note:** Defense-in-depth is now consistent — both literal backslash and URL-encoded backslash are explicitly checked.

## Android-18 Commit Review e866df5f — Non-Code

**Date:** 2026-03-07  
**Review by:** Android-18 (K2.5)  
**Commit:** `e866df5f`

**Status: NO CODE CHANGES**

Documentation-only commit adding Android-18 commit review findings for recent batches.

## Android-18 Commit Review 60b9b445 — Non-Code

**Date:** 2026-03-07  
**Review by:** Android-18 (K2.5)  
**Commit:** `60b9b445`

**Status: NO CODE CHANGES**

Documentation-only commit adding Android-18 review findings for earlier batches.

## Android-18 Commit Review 2bfb8220 — Non-Code

**Date:** 2026-03-07  
**Review by:** Android-18 (K2.5)  
**Commit:** `2bfb8220`

**Status: NO CODE CHANGES**

Documentation-only commit logging Android-17 evening fixes.

---

## Android-18 Commit Review (Session)

### a75a5bb4 — Privacy Shield Service ID Fix
**Status: Committed and merged**

**Issues fixed:**
- Critical: `reset_model_state` fixture now uses `server._models_loaded` instead of `global _models_loaded`
- High: Service ID correctly set to `privacy_shield` (snake_case) to match directory name
- Medium: Test imports use `from extensions.services.bark import server` for proper module reference
- Medium: `detect-hardware.sh` restored `| head -1` for multi-GPU compatibility
- Low: Completed truncated test, added `test_load_models_thread_safety`, added `import numpy as np`
- Low: Refactored to use module reference instead of direct underscore-globals import

### 04374d0a — Multi-GPU Detection in Installer
**Status: Committed and merged**

**Issues fixed:**
- MEDIUM: Removed duplicate `GPU_COUNT` assignment (was on lines 95 and 100)
- MEDIUM: Fixed fragile PCI ID extraction — reverted from `xargs` + `${pci_ids:0:6}` to `head -1 | xargs` for safer single-GPU ID extraction

### e5dbdf8e — Text Generation WebUI Healthcheck & RVC Workflow
**Status: Committed and merged**

**Issues fixed:**
- HIGH: RVC Discord node syntax — removed backticks, wrapped entire expression with `={{ }}`
- MEDIUM: RVC HTTP nodes now include `body` parameters for file upload and model parameters
- MEDIUM: text-generation-webui healthcheck reverted from `CMD-SHELL` to `CMD` array (secure)
- LOW: RVC workflow uses standard `$json` instead of `$item().json` for previous node output

---

## Git Activity (Final)
- `952a59b5` — fix(rvc): restore backslash check for Windows path traversal defense-in-depth
- `e1467738` — fix(service-registry): fail fast on _safe_cd errors
- `2860cf86` — fix(rvc): remove redundant backslash check in model path validation
- `f3a07946` — fix: replace deprecated asyncio.get_event_loop() with get_running_loop()
- `89f31dd4` — fix(rvc): restore leading slashes in workflow paths
- `48275914` — fix(review): address K2.5 code review batch
- `4e273d5c` — fix(rvc): add case-insensitive URL encoding check
- `3732fd3e` — fix(tests): address Android-18 review findings for test_server.py
- `04374d0a` — fix(hardware): detect multi-GPU setups in installer
- `e5dbdf8e` — fix: Text Generation WebUI healthcheck — use wget with shell command
- `b196cc70` — fix(workflows): correct n8n Code node property and add RVC security validation
- `b5abebdd` — fix(rvc): remove leading / from path params
