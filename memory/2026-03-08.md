# 2026-03-08 — QA Review Session (00:04 EST)

**Review trigger**: Android-18 ping for commit review
**Commits reviewed**: `37a9099e`, `3a88c534`, `2dead1b0` (Bark TTS extension)

## Review Findings

### 37a9099e — docs(bark): fix comment: exact match, not prefix match
✅ **APPROVED** — Comment now accurately describes the exact string matching implementation.

### 3a88c534 — fix(bark): add voice_preset validation to server and test
✅ **APPROVED** — Validation correctly:
- Whitelists all documented Bark v2 voices (13 languages × 10 speakers)
- Returns 422 for invalid presets
- Prevents injection of arbitrary voice strings
- Test coverage for validation errors

### 2dead1b0 — test(bark): add production tests for TTS server
✅ **APPROVED** — Comprehensive test coverage:
- Health endpoint (pre/post model load)
- TTS success paths (default format, case-insensitive format)
- Validation errors (invalid format, text too long)
- Model loading on first request
- Concurrent request handling
- Thread safety for model loading
- Error handling (generation failures)

## Android-18's LOW Issues (Style/Coaching)
1. **Inline patch usage**: Tests use `patch(..., True)` inside functions — prefer `@patch` decorator or fixtures for pytest idioms
2. **Mock data realism**: `mock_soundfile_write` uses fake bytes — could use valid WAV header for more realistic tests

**Verdict**: These are style preferences, not functional issues. Code is production-ready.

## GitHub Issues Status
| Issue | Status | Notes |
|-------|--------|-------|
| #33 Dashboard offline | Fix committed (8ea4bd2e), pending promotion | In dev/main, needs Michael to push to public |
| #55 Dual GPU | OPEN | Hardware detection issue — 16's area |
| #32 Windows install | OPEN | Missing PowerShell script — unassigned |
| #22 Gateway security | Not a bug | Intentional localhost binding verified |

## Bark TTS Server Tests (Commit `3a88c534`)

**Issue:** Android-18 flagged missing input validation for `voice_preset` (commit `2dead1b0`).

**Fix:** 
- Added `VALID_VOICES` set with all 130 Bark v2 presets
- Added `@field_validator('voice_preset')` to TTSRequest model
- Returns 422 for invalid presets (instead of runtime 500 error)
- Test updated to verify validation

**Minor issue:** Comment said "prefix match" but code uses exact string matching - implementation is correct.
**Fixed in commit `37a9099e`:** Comment updated to "exact match, not prefix match"

---

## Next Actions
1. Pick up #32 (Windows install) or assist with #55 (dual GPU)
2. Continue extension wave when issues clear

**Review queue status**: CLEAR ✅
