# 2026-03-08 — Session Log

## Commit Review: `6fb72909` (Revert)
- Issue: Android-18 reviewed a revert commit that was already superseded
- Current AMD overlay (`362afed9`) is clean — minimal overlay pattern with only AMD-specific volumes
- Revert chain: `9c4bf1e2` → `f2951ed4` → `362afed9` (final)

## Whisper Entrypoint Fix
- Commit: `245b42ac` — hardcode `DREAM_PATCHED`, remove `PATCH_MARKER` variable
- Removed variable interpolation to eliminate escaping fragility
- Both perl and sed commands now use hardcoded marker

## n8n Template Verification
- `webhook-trigger.json`: ✅ Has `Respond to Webhook` node properly connected
- `scheduled-task.json`: ✅ No webhook nodes, clean
- Timeout fix: `90b6d2a4` — corrected from 30ms to 30000ms (30s)

## Open-Interpreter Review (`c159720c`)
- Android-18 flagged missing security settings — **false positive**
- File already has: `healthcheck`, `security_opt: no-new-privileges`, `deploy.resources.limits`
- Commit only fixed build context path

## safeCompare Vulnerability (Flowise/Langflow)
- Commit `b463b719` / `fc41572b`: Introduced padding-based comparison
- Issue: Null-byte padding unsafe if keys contain `\0`; redundant and fragile
- Fix: Revert to original constant-time comparison without padding
- Pattern:
  ```js
  function safeCompare(a, b) {
    if (a.length !== b.length) return false;
    if (a.length > MAX_KEY_LENGTH) return false;
    let result = 0;
    for (let i = 0; i < a.length; i++) {
      result |= a.charCodeAt(i) ^ b.charCodeAt(i);
    }
    return result === 0;
  }
  ```

## GitHub Issue Status
- #55 (dual GPU), #32 (Windows), #33 (deployment verify) pending
- Hardening wave and installer testing in progress
