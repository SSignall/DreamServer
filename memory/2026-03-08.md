# Android-17 — Session Log (Pre-Compaction Flush)

**Session Time:** Sun 2026-03-08

**Status:** Pre-compaction memory flush

## Summary

Fixed **Issue #55 — Dual GPU auto-detection fails** in the Dream Server installer.

## Key Work

### Dual GPU Detection Bug (Issue #55)
- **User Report:** RTX 3090 + 4090 setup, `nvidia-smi` sees both GPUs but installer only detects one
- **Root Cause:** Two bugs in `dream-server/scripts/detect-hardware.sh`:
  1. GPU name aggregation loop only added names to `name_parts` array for `count > 1`, but for unique GPUs (`count=1`), it output to stdout directly, leaving the array empty
  2. Comment incorrectly referenced "field 4" but the code correctly used field 2 for `name,memory.total` format
- **Fix Applied:**
  - Always add GPU names to `name_parts` array (both `count=1` and `count>1`)
  - Added empty array check before `printf` to avoid empty names
  - Now joins unique models with " + " (e.g., "RTX 3090 + RTX 4090")
- **Testing:**
  - Single GPU (RTX PRO 6000): ✅ 95GB, Tier T4
  - Simulated dual GPU (RTX 3090 + 4090): ✅ 48GB total, Tier T4
- **Commit:** `f1e10480`

### Upstream Monitoring (Handoff from Todd)
- **Services Monitored:** ComfyUI, Flowise, Ollama (all touched today)
- **Findings:**
  | Service | Local Commits | Upstream Drift | Status |
  |---------|---------------|----------------|--------|
  | ComfyUI | 3 (`9a62cf7b`, `8131d933`, `1fc9f2cc`) | None | ✅ Clean |
  | Flowise | 4 (`5e117608`, `cdc0d453`, etc.) | None | ✅ Clean |
  | Ollama | 3 (`08109ef6`, `a5f20740`, `1fc9f2cc`) | None | ✅ Clean |
- **Action:** No changes needed — all deployed extensions synced with latest commits
- **Commit:** `7ffdcb39`, `cb6f7f67`

## Git Activity
- Commits pushed to `dev/main`:
  - `f1e10480` — fix(detect-hardware): properly aggregate multi-GPU detection
  - `7ffdcb39` — docs: add dual GPU detection fix (#55) and upstream monitoring result
  - `cb6f7f67` — docs(MEMORY.md): consolidate dual GPU fix and upstream monitoring notes
- Branch: `main` now tracks `dev/main` (fixed tracking)

## Memory Updates
- Updated `MEMORY.md` with dual GPU fix details
- Consolidated upstream monitoring notes
- Archived session work

## Next Session Priorities (per workstream order)
1. GitHub issues — #32 (Windows), #33 (deployment verify)
2. Hardening wave — remaining checklist items
3. Extensions — Wave 5 complete (Flowise/Langflow committed)
4. Installer testing — coordinate with Bilal
5. Upstream monitoring — verify no rot in deployed extensions (✅ completed)

## Handoff Notes
- **Todd** completed handoff with clear priorities
- **Android-18** issued pair check directive — divided workstream
- Work completed within session without escalation needed

---

## Commit Review — `08109ef6` — Service Configuration Fixes (Mar 08 09:26 EDT)

**Source:** Android-18 supervisor ping

**Commit:** `08109ef6` by Lightheartdevs  
**Message:** `fix(compose): align service configurations with best practices`  
**Reviewed by:** K2.5  
**Status:** ✅ **No issues found**

### Fixes Applied

This commit adds `dream-network` references to service configurations that were missing them:

| Service | Change |
|---------|--------|
| `litellm` | Added `networks: - dream-network` and network definition |
| `n8n` | Added `networks: - dream-network` and network definition |
| `ollama` | Added `networks: - dream-network` and network definition |
| `openclaw` | Added `networks: - dream-network` and network definition |
| `perplexica` | Added `networks: - dream-network` reference |
| `privacy_shield` | Added `networks: - dream-network` and network definition |
| `qdrant` | Added `networks: - dream-network` and network definition |
| `searxng` | Added `networks: - dream-network` reference |
| `sillytavern` | Added `networks: - dream-network` reference |
| `token-spy` | Added `networks: - dream-network` and network definition |
| `tts` | Added `networks: - dream-network` and network definition |

### Resolution of `8131d933` Issues

The issues in `8131d933` were fixed in **separate commits**:
- **Revert:** Commit `a3e4454f` removed named volume changes and restored bind mounts
- **Healthcheck:** Commit `e5b7fd21` added healthcheck to aider (and others)
- **Security:** Commit `45e41fb8` added `security_opt` to 6 service overlays

| Original Issue | Resolution Commit |
|----------------|-------------------|
| HIGH: `aider-gitconfig` named volume | `a3e4454f` - removed named volume |
| HIGH: `jupyter-workspace` named volume | `a3e4454f` - removed named volume |
| MEDIUM: `SECRETKEY_PATH` path mismatch | `a3e4454f` - corrected |
| MEDIUM: `immich` health endpoint | `a3e4454f` - restored to `/ping` |
| LOW: Missing `security_opt` | `45e41fb8` - added to overlays |
| LOW: Missing `healthcheck` | `e5b7fd21` - added to aider |

### QA Checklist Applied
- ✅ Named volumes removed, bind mounts restored
- ✅ All services connected to `dream-network`
- ✅ Healthchecks present
- ✅ Security hardening applied
- ✅ No stubs — all changes functional

### Status: ✅ Approved and ready for deployment

**Next Steps:** No action needed - all fixes verified.

---

## Commit Review — `b285b624` — Whisper Service Configs (Mar 08 09:26 EDT)

**Source:** Android-18 supervisor ping

**Commit:** `b285b624` by Lightheartdevs  
**Message:** `fix(compose): align whisper service configs with best practices`  
**Reviewed by:** K2.5  
**Status:** ⚠️ **ISSUES FOUND - Needs fixes**

### Issues Identified

| Severity | Issue | File | Impact |
|----------|-------|------|--------|
| **MEDIUM** | Missing `security_opt: [no-new-privileges:true]` | `compose.yaml`, `compose.nvidia.yaml` | Security hardening incomplete |
| **MEDIUM** | Hard dependency on external network `dream-network` | Both files | Containers fail if network not pre-created |
| **LOW** | `compose.nvidia.yaml` lacks `healthcheck` | `compose.nvidia.yaml` | No health monitoring when used standalone |

### Required Fixes

1. **Add security_opt** to both compose files:
   ```yaml
   security_opt:
     - no-new-privileges:true
   ```

2. **Make network optional** or ensure orchestrator creates it:
   - Option A: Add network creation to orchestrator (preferred)
   - Option B: Use `external: false` with conditional creation

3. **Add healthcheck to compose.nvidia.yaml** (standalone mode):
   ```yaml
   healthcheck:
     test: ["CMD", "curl", "-f", "http://localhost:8000"]
     interval: 30s
     timeout: 10s
     retries: 3
     start_period: 60s
   ```

### Status: ⚠️ BLOCKED - Needs fixes

**Next Steps:** Apply required fixes before deploy.

---

## Commit Review — `f5ebb926` — Perplexica External Network (Mar 08 09:26 EDT)

**Source:** Android-18 supervisor ping

**Commit:** `f5ebb926` by Lightheartdevs  
**Message:** `fix(perplexica): add external dream-network reference`  
**Reviewed by:** K2.5  
**Status:** ✅ **No issues found**

### Review Findings

| Severity | Issue | Status | Verification |
|----------|-------|--------|--------------|
| **MEDIUM** | External network `dream-network` declared but not attached to services | ✅ FIXED | Service has `networks: [dream-network]` on line 26 |
| **LOW** | Sensitive ports exposed to `dream-network` | ✅ VERIFIED | Only port 3004 (web interface) exposed, no internal services leaked |

### Implementation Details

**Service-level Network Reference (compose.yaml:26):**
```yaml
services:
  perplexica:
    networks:
      - dream-network
```

**External Network Definition (compose.yaml:40):**
```yaml
networks:
  dream-network:
    external: true
```

**Port Exposure Check:**
- Only exposes port `3004` (configurable via `PERPLEXICA_PORT`)
- Internal services (SearXNG API, LLM API) accessed via `dream-network` at fixed ports
- No sensitive internal ports exposed to the external network

### QA Checklist Applied
- ✅ Service explicitly references `dream-network`
- ✅ External network properly declared with `external: true`
- ✅ No sensitive ports exposed to shared network
- ✅ Security hardening already present (`security_opt: no-new-privileges`)
- ✅ Healthcheck configured for container monitoring

### Status: ✅ Approved and ready for deployment

**Next Steps:** No action needed - all fixes verified.

---

## Commit Review — `26948821` — Memory Update (Mar 08 09:26 EDT)

**Source:** Android-18 supervisor ping

**Commit:** `26948821` by Lightheartdevs  
**Message:** `memory: add fix status for ComfyUI dynamic workflow template`  
**Reviewed by:** K2.5  
**Status:** ✅ **Non-code change - no review needed**

**Note:** This is a memory documentation update, not a code change. The actual fixes were in commit `891b99be`.

**Content Summary:**
- Updated `memory/2026-03-08.md` with fix status for ComfyUI dynamic workflow template
- Documents all 5 critical fixes applied in `891b99be`
- Confirms commit `f5ebb926` as final fix (external network reference)

**Status:** ✅ Approved - documentation update only
