# Memory — 2026-03-08

## Security Hardening Audit

**Task:** Compare RVC, Whisper, and Bark n8n workflows for security patterns.

**Findings:**
- **RVC:** Heavily hardened—timingSafeEqual auth, path traversal protection, model/channel/pitch/f0_method validation
- **Whisper:** No auth or input validation—missing security layer
- **Bark:** No auth or input validation—missing security layer

**Recommendation:** Apply RVC-style security to Whisper and Bark workflows.

## LocalAI Workflow Hardening

**Committed:** `2e401ab6` (`main` on `dev` remote)

**Changes:**
- API key authentication (`LOCALAI_API_KEY` env var, `timingSafeEqual`)
- Input validation:
  - Message: required, max 32768 chars
  - Model: optional, alphanumeric/dash/underscore only, max 100 chars
  - Temperature: 0-2 range, default 0.7
  - Max tokens: 1-32000, default 1024
  - Channel: alphanumeric/dash/underscore only
- Discord error output shows validation requirements

## Extension Validation

**Command:** `make validate-ext` in `dream-server/`

**Results:**
- 445 passed, 0 failed, 1 warning (port conflicts—expected)
- All extensions have valid manifests, pinned images, healthchecks, `security_opt`, resource limits
- Port conflicts: Known design (services that can't run simultaneously)

## Git Workflow

- Push all work to `dev/main` branch only
- Use `git push dev main`
- Never push to origin, create PRs, or feature branches for public repo

## Workstream Progress

- ✅ RVC fixes committed (17-16 review follow-up)
- ✅ Extension validation clean
- ✅ LocalAI security hardening committed
- ⏳ Whisper/Bark security hardening (not yet implemented)
