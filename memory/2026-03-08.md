# 2026-03-08 — Session Log

## Work Completed

### n8n Workflows (3 shipped)

| Workflow | Commit | Status |
|----------|--------|--------|
| Bark TTS | `980dceaf` | ✅ Approved after 3 review cycles |
| LocalAI | `99c86e1b` | ✅ Approved |
| ComfyUI | `9eb284d7` | ✅ Just shipped |

**Bark TTS Fixes:**
- Critical routing bug: Process Response was receiving wrong input
- Fixed via `$input.all()` pattern — Bark API response + Webhook Trigger inputs
- Error truncation added to prevent Discord 2000 char limit failures
- Final: 3 nodes (was 6), clean pattern

### Git Workflow
- All work pushed to `dev/main` on Lighthouse-AI-Dev repo
- Pre-push hook enforces sync with remote before push

## Review Notes (Android-18)
- `303598cd`: [CRITICAL] Logic error in routing flagged
- `da58b24b`: [HIGH] `$input` reference bug, [MEDIUM] redundant processing
- All issues resolved in subsequent commits

## Review Cycle (Sun 2026-03-08)

### Commits Reviewed
| Commit | Issue | Status |
|--------|-------|--------|
| `e2967cbd` | Memory update | ✅ |
| `dfa5ea5a` | ComfyUI fixes | ⚠️ K2.5 flagged critical issues |
| `8695c69f` | ComfyUI issues | ✅ Superseded by later fixes |
| `b09f5e06` | Installer test | ✅ |
| `9eb284d7` | ComfyUI base | ✅ Superseded by fixes |

### Fixes Applied
| Commit | Fix | Impact |
|--------|-----|--------|
| `2bf594d1` | Added Validate Input node to ComfyUI | Input validation, dimension clamping |
| `2bf594d1` | Fixed ComfyUI error handler pattern | Proper `$input.all()[0]?.error?.message` |
| `2bf594d1` | Fixed LocalAI error handler pattern | Corrected n8n syntax for error access |
| `f854e614` | Restored Validate Input node connections | Webhook → Validate → Queue → Process |
| `f854e614` | Fixed Process Response payload access | Uses `$('Validate Input')` instead of `allInputs[1]` |

### Critical Issues Found (K2.5)
- `dfa5ea5a`: Validate Input node removed, breaking input validation
- `dfa5ea5a`: Process Response uses wrong payload source (`allInputs[1]`)
- `dfa5ea5a`: Error handler references non-existent node
- All fixed in `f854e614`

### Commits Pushed (dev/main)
| Commit | Message |
|--------|---------|
| `eb01f5a4` | Error handler channel reference and seed parsing fix |
| `597a19f1` | parseInt radix, seed=0 handling |
| `2180deca` | Memory update with K2.5 findings |
| `f854e614` | ComfyUI workflow connection restoration |

## Next Session Priorities
1. GitHub issues — #32 (Windows), #22 (gateway), #55 (dual GPU), #33 (deployment verify)
2. Hardening wave — remaining checklist items
3. Extensions — Flowise, Dify, or other high-value targets
4. Installer testing — coordinate with Bilal
5. Upstream monitoring — verify no rot in deployed extensions
