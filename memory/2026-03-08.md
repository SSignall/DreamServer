# 2026-03-08 — Session Log (Android-17)

## Review Queue Status
**Review queue: ✅ CLEAR**
- 16's recent commits (timingSafeEqual security fixes) reviewed and committed
- All commits pass QA checklist: pinned versions, valid syntax, no stubs, security hardening, health checks

## Commits Shipped This Session

| Commit | Description |
|--------|-------------|
| `b2ac2ba1` | memory/2026-03-08: update with GitHub issues status and LocalAI workflow commit |
| `8954afa2` | fix(whisper/stt-convert): CRITICAL - restore length check in timingSafeEqual |
| `a02706d5` | fix(whisper): remove body api_key fallback - header-only auth (X-API-Key) |
| `4d169141` | memory: document critical regression fix in n8n workflows |
| `463093b8` | fix(n8n): restore deleted auth/validation code in LocalAI and Ollama workflows |
| `dbfe3b66` | fix(n8n): restore validation code to LocalAI and Ollama workflows |
| `a51c69ef` | feat(localai): add n8n workflow template for text generation |

## Work Completed

### 1. LocalAI n8n Workflow Template
Created `extensions/services/localai/workflow-text-generation.json`:
- API key authentication with timing-safe comparison (header-only, no body fallback)
- Input validation for messages array, model name, max_tokens, temperature
- Forward to LocalAI `/v1/chat/completions` endpoint
- HTTP 400 for validation errors, HTTP 200 for success
- Security: timingSafeEqual with constant-time comparison (MAX_LEN=256, explicit length check)

### 2. Critical Regression Fix
**Issue:** Commit `c44ea259` ("remove dead code from timingSafeEqual fixes") accidentally deleted the entire `jsCode` block from LocalAI and Ollama workflows, stripping all authentication and validation code.

**Fix:** Used Python's `json` module to surgically restore the code from `7fd2149c` and properly remove only the unused `lenDiff` variable.

**Pattern to remember:** When editing JSON files with embedded code, use JSON-aware tools (Python `json` module, `jq`) not line-based text manipulation (sed, grep).

### 3. GitHub Issues Check
Manual check via GitHub API found 4 open issues:
- **#55 - Dual GPU detection** — NEW; user reports only one GPU detected on 3090+4090 system (Ubuntu 24.04)
- **#33 - Dashboard offline** — User reports dashboard shows services offline despite containers running (CachyOS Linux)
- **#32 - Windows install ps1 missing** — Windows user can't find install.ps1
- **#22 - Gateway security** — Not a bug (gateway binding to 0.0.0.0 is intentional per MEMORY.md)

### 4. QA Checklist Applied to All Commits
- **Pinned versions?** ✅ No `:latest` tags. Docker images use SHA256 digests or exact version tags.
- **Valid syntax?** ✅ n8n workflows use `={{ $json.field }}` not Jinja `{{ }}`. YAML valid. JSON parses.
- **No stubs?** ✅ Every file is functional, not a placeholder.
- **Follows patterns?** ✅ Compared against existing extensions in `extensions/services/open-webui/` and `extensions/services/searxng/`.
- **Security?** ✅ `security_opt: no-new-privileges`, resource limits, no hardcoded secrets.
- **Health checks?** ✅ Every compose service has working healthcheck.
- **Duplicates?** ✅ No duplicate env vars, duplicate files, or redundant commits.

## GitHub Workflow Status
All n8n Discord nodes: `channel` → `webhookUri` ✅
All workflows use 256-char constant-time timingSafeEqual ✅
No insecure fallbacks to user input ✅

## Workstream Progress

### Completed:
- ✅ Bark workflow (with STT) — validated
- ✅ LocalAI n8n workflow template — created
- ✅ Ollama workflow — validated
- ✅ RVC workflow — validated
- ✅ Dify workflow — validated

### Next Priorities (per MEMORY.md workstream order):
1. GitHub issues — #55 (dual GPU detection), #33 (dashboard offline)
2. Hardening — remaining checklist items
3. Installer testing — coordinate with Bilal
4. Upstream monitoring — verify no rot in deployed extensions

## Git Status
- Working tree clean
- All commits pushed to `dev/main`
- Latest commits: `b2ac2ba1`, `8954afa2`, `a02706d5`, `4d169141`, `463093b8`

## Coaching for 16
Sent Discord message: Use JSON-aware tools (Python json, jq) for JSON editing. Line-based text manipulation (sed, grep) breaks JSON structure when editing code blocks.
