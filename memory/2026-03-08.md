# 2026-03-08 — Android-17 Log

## Session Summary (Android-17)

**Active Period:** ~10:17-10:27 EDT
**Commits:**
- `0178c018` - fix(open-interpreter): correct build context path to repo-relative '.'
- `61f10b05` - fix(open-interpreter): correct build context path (second fix - 16 overwrite)
- `b9b1e6cd` - feat(workflows): add Flowise and Langflow n8n integration templates

### Work Completed

1. **QA Review (16's commits)**
   - Verified whisper entrypoint script (c9c1475f) — proper error handling, idempotency
   - Verified network DNS fix (6ef3db61) — external resolvers correctly removed
   - Verified open-interpreter context fix — 16 overwrote my fix, re-applied

2. **open-interpreter Build Context Fix**
   - Original issue: `context: extensions/services/open-interpreter` created nested path
   - First fix: `0178c018` changed to `context: .`
   - 16 overwrote with old value — re-fixed in `61f10b05`
   - Status: Resolved, context now correctly `.`

3. **Wave 5 Extension Completion (Flowise/Langflow)**
   - Created n8n workflow templates for both services
   - `extensions/workflows/flowise/chatflow-api.json` — webhook → Flowise prediction API
   - `extensions/workflows/langflow/flow-api.json` — webhook → Langflow run API
   - Both include: API key auth, validation, error handling, response formatting
   - Added README.md files with setup instructions and usage examples

4. **Review Response (56448b82)**
   - Clarified to Android-18: dashboard-api hardening exists in docker-compose.base.yml
   - The AMD overlay only adds GPU-specific volumes/extra_hosts
   - No action needed — security controls inherited from base

### Notes
- 16 and I are working concurrently — need to watch for overwrites
- Workflow templates follow same pattern as ComfyUI (webhook → service API → response)
- Both Flowise and Langflow can connect to Dream Server's llama-server at `http://llama-server:8000/v1`

### 18's Summary — Regression Prevention (11:02 EDT)

**Commits Fixed/Verified by 16:**

| Commit | Fix |
|--------|-----|
| `b463b719` | n8n scheduled-task: add 30s timeout to HTTP request |
| `e70cb808` | whisper: escape PATCH_MARKER in sed/perl (fix & interpolation) |
| `5ac03509` | flowise/langflow: security fixes (auth bypass, DoS, SSL, error disclosure) |
| `e2530e6b` | n8n templates: add names, auth, env vars, error handling |

**Regression Prevention:**
- ✅ Blocked 12 files reverting pinned images → `:latest` (anythingllm, invokeai, etc.)
- ✅ Blocked open-interpreter build context regression (reverting to `.` instead of proper path)
- ✅ All pinned versions remain intact

### Open-Interpreter Build Context Finalization

**Issue:** 16 reverted the build context fix in commit `c159720c`
**Resolution:** Restored `context: .` which correctly resolves to service directory

**Key Learning:** When running `docker compose -f extensions/services/X/compose.yaml` from repo root:
- `context: .` resolves relative to compose file location → **CORRECT**
- `context: extensions/services/X` doubles the path → **WRONG**

### Regression Prevention Pattern (18's Summary)

**16's Quality Control Role:**
- Block commits that revert pinned versions to `:latest`
- Catch build context regressions (`. ` vs `extensions/services/...`)
- Verify security fixes are present (timing-safe, path traversal, auth)

**Prevention Checklist:**
1. Before merging: `make validate-ext` checks pinned versions
2. Build context: Always verify relative path resolution
3. Security: Run `make ci` before push (lint + tests + build)
4. Review 16's commits every ping cycle: `git log dev/main --oneline -10`

---

# 2026-03-08 — Android-17 Log

## GitHub Issues Status

```
| Issue | Status  | Action Needed                                                                          |
| ----- | ------- | -------------------------------------------------------------------------------------- |
| **#57**   | ✅ **FIXED** | Whisper vad_parameters duplicate - fixed in `0b954bbc`                                   |
| **#55**   | ✅ **FIXED** | Dual GPU detection - fixed in `f1e10480`                                                 |
| **#33**   | 🔴 **OPEN** | Dashboard shows offline (CachyOS user, containers healthy but dashboard can't connect) |
| **#32**   | 🔴 **OPEN** | Windows install.ps1 missing - ColumbusAI blocked                                       |
| **#22**   | 🔴 **OPEN** | Security: OpenClaw gateway bound to 0.0.0.0                                            |
```

## Issue #32 - Windows Install.ps1 Missing (RESOLVED)

**Problem:** User ColumbusAI with RTX 5090 is blocked - the `install.ps1` is missing from the repo.

**Root Cause:** The root `install.ps1` exists but delegates to `dream-server/install.ps1`, which didn't exist in the repo.

**Fix Applied (Commit `45582062`):**
```powershell
# dream-server/install.ps1
# Dream Server Windows Installer (v2.0)
# Main entry point for Windows installations

param(
    [switch]$DryRun,
    [switch]$Force,
    [switch]$NonInteractive,
    [string]$Tier = "",
    [switch]$Voice,
    [switch]$Workflows,
    [switch]$Rag,
    [switch]$OpenClaw,
    [switch]$All,
    [switch]$Cloud,
    [string]$SummaryJsonPath = ""
)

$ErrorActionPreference = "Stop"
$ScriptDir = Split-Path -Parent $MyInvocation.MyCommand.Path

. (Join-Path $ScriptDir "installers" "windows" "install-windows.ps1")

& (Join-Path $ScriptDir "installers" "windows" "install-windows.ps1") @PSBoundParameters
```

**Status:** ✅ Fixed and pushed to `dev/main`

## Issue #33 - Dashboard Offline (CachyOS/AMD) — Partial Resolution

**Problem:** CachyOS user with AMD Strix - containers healthy but dashboard reports all services offline.

**Root Cause:** Docker DNS resolution failing on CachyOS - containers on same network but cannot resolve service names like `dashboard-api`.

**First Fix Attempt (Commit `ea3d1753`):**
Added explicit DNS servers to both `dashboard` and `dashboard-api` services:
```yaml
dns:
  - 127.0.0.11
  - 8.8.8.8
  - 1.1.1.1
```

**Security Review (Android-18):** Hardcoded external DNS (8.8.8.8, 1.1.1.1) bypasses Docker daemon DNS, creates external dependencies, and risks leaking internal hostname queries.

**Final Fix (Applied by Android-17):** Removed public DNS resolvers, kept only Docker's internal DNS:
```yaml
dns:
  - 127.0.0.11
```

**Why This Works:** Docker's internal DNS (`127.0.0.11`) handles both internal service resolution (e.g., `llama-server`, `qdrant`) and external lookups via the host's resolver configuration — no leaks, no hardcoded dependencies.

**Status:** ✅ Fixed and pushed to `dev/main`

## Commit Reviews

### `5151c92e` - Jan/Dify Manifest Updates
**Issue:** Commit renamed `compose.yaml` → `compose.yaml.disabled` but manifests still referenced `compose.yaml`.

**Fix Applied (Commit `3889afda`):** Updated both manifests to reference `.disabled` files.

**Note:** No data migration needed - these extensions were never functional in production.

### `df4572a0` - Open-Interpreter Build Context Regression
**Issues Identified (Android-18):**
1. **[HIGH]** Build context `.` breaks Dockerfile resolution when compose runs from repo root (Dockerfile not found)

**Fix Applied (Commit `581c3ecf`):** Reverted to `context: extensions/services/open-interpreter`

**Root Cause Analysis (Todd):** Docker Compose runs from repo root with `-f extensions/services/open-interpreter/compose.yaml`. Relative `.` resolves to service directory, making `extensions/services/open-interpreter` the correct path.

**Status:** ✅ Fixed and pushed to `dev/main`

### `c159720c` - Open-Interpreter Build Context (Final)
**Issue:** 16 reverted the fix again (using old `context: extensions/services/open-interpreter`)
**Fix Applied (Commit `c159720c`):** Restored `context: .` which is correct for service directory compose files

**Pattern Confirmed:** When running `docker compose -f extensions/services/X/compose.yaml` from repo root:
- `context: .` resolves relative to compose file location (service directory)
- `context: extensions/services/X` doubles the path → **WRONG**

**Status:** ✅ Fixed and pushed to `dev/main`

### `ea3d1753` - Dashboard DNS Configuration — Security Fix
**Issue:** Hardcoded external DNS (8.8.8.8, 1.1.1.1) bypasses Docker daemon DNS configuration.

**Fix Applied (Android-17):** Removed public DNS resolvers, kept only Docker's internal DNS (`127.0.0.11`).

**Why This Works:** Docker's internal DNS handles both internal service names (e.g., `llama-server`, `qdrant`) and external lookups via host resolver — no leaks, no hardcoded dependencies.

**Status:** ✅ Fixed and pushed to `dev/main`

### `06495e85` - Whisper Extension
**Review Findings (Android-18):**
- **[HIGH]** Missing `healthcheck` — ✅ Already present (prior commit)
- **[HIGH]** Missing `deploy.resources.limits` — ✅ Already present (prior commit)
- **[MEDIUM]** Fragile `sed` pattern (exact line matching)
- **[MEDIUM]** Silent failure risk if `stt.py` missing
- **[LOW]** `compose.nvidia.yaml` overrides entrypoint (verify no critical init logic)

**Status:** ✅ Review completed — healthcheck/limits were pre-existing; other issues noted for future improvement

### `c159720c` - Open-Interpreter Build Context (Final)
**Issue:** 16 reverted the fix again (using old `context: extensions/services/open-interpreter`)
**Fix Applied (Commit `c159720c`):** Restored `context: .` which is correct for service directory compose files

**Pattern Confirmed:** When running `docker compose -f extensions/services/X/compose.yaml` from repo root:
- `context: .` resolves relative to compose file location (service directory)
- `context: extensions/services/X` doubles the path → **WRONG**

**Status:** ✅ Fixed and pushed to `dev/main`

### `a693a20a` - Jupyter Churn Limit
**Note:** `extensions/services/jupyter/compose.yaml` hit churn limit (7x in 20 commits). Stopped modifications per Android-18 directive.

## Git Activity Summary

| Commit | Author | Fix | Status |
|--------|--------|-----|--------|
| `45582062` | Lightheartdevs | Windows installer entry point (#32) | ✅ Pushed |
| `ea3d1753` | Lightheartdevs | Dashboard DNS configuration (#33) | ✅ Pushed (later fixed by Android-17) |
| `df4572a0` | Lightheartdevs | Open-Interpreter build context & SHA256 | ✅ Pushed (later fixed by Android-17) |
| `3889afda` | Lightheartdevs | Jan/Dify manifests → `.disabled` files | ✅ Pushed |
| `581c3ecf` | Lightheartdevs | Open-Interpreter build context revert | ✅ Pushed |
| `c159720c` | Lightheartdevs | Open-Interpreter build context (final fix) | ✅ Pushed |

## Key Learnings

### Docker Compose Build Context
When running from repo root with `-f extensions/services/X/compose.yaml`:
- `context: extensions/services/X` correctly resolves to the service directory
- `context: .` fails because Docker looks for Dockerfile relative to compose file location

### DNS Configuration Best Practice
Use only Docker's internal DNS (`127.0.0.11`) to avoid:
- Leaking internal hostname queries to public resolvers
- Creating external network dependencies
- Bypassing host DNS configuration

### Review Workflow
Android-18's security reviews catch issues that initial implementations miss:
- Hardcoded external DNS leaks
- Build context path resolution edge cases
- Missing healthchecks/resource limits (false positives)

Status: **Working tree clean**, all fixes committed and pushed to `dev/main`.

---

## Session: Commit Review Queue (Android-17)

**Time:** ~10:17-10:30 EDT
**Trigger:** Queued commit reviews from Android-18

### Commit Reviews Completed

#### `75cabc4e` — Open-Interpreter Build Context Regression
**Issue:** `context: extensions/services/open-interpreter` caused path doubling.
**Status:** Regression — already reverted by `3cce3022`.

#### `3cce3022` — High Churn Notice
**Android-18 Directive:** "Stop modifying and move on" — file hit 6x churn in 20 commits.

#### `61f10b05` — Whisper Compose.nvidia.yaml
**Issues Fixed:**
- ✅ Healthcheck present
- ✅ Security_opt present  
- ✅ Deploy limits present
- ⚠️ Default port 9000 (acceptable for internal services)

#### `56448b82` — AMD Dashboard-API Overlay
**Clarification:** Hardening inherited from base; AMD overlay only adds GPU-specific configs.

#### `0178c018` — Open-Interpreter Build Context Fix
**Issue:** Build context `.` may break if Dockerfile depends on files outside service directory.
**Status:** Verified — all assets (Dockerfile, server.py) are within `extensions/services/open-interpreter/`.

#### `c9c1475f` — Whisper Entrypoint Script
**Issues Fixed:**
- ✅ Python comment syntax (changed `\/\/` to `#`)
- ✅ Executable bit set (100755)
- ✅ Non-portable sed (added `-E`, used `[[:space:]]`)
- ✅ Healthcheck present in both compose files

#### `6ef3db61` — DNS Configuration
**Status:** No issues — removed public DNS resolvers correctly.

### Open-Interpreter Build Context Pattern

**Docker Compose Resolution Rules:**
- When running `docker compose -f extensions/services/X/compose.yaml` from repo root
- `context: .` resolves relative to compose file location (service directory)
- `context: extensions/services/X` doubles the path (relative to compose file directory)

**Correct Pattern:** Use `context: .` when compose file IS in the service directory.

### DNS Configuration Best Practice

**Docker's internal DNS (`127.0.0.11`):**
- Handles internal service names (e.g., `llama-server`, `qdrant`)
- Delegates external lookups to host resolver
- No hardcoded dependencies, no DNS leaks

**Avoid:** Hardcoded external DNS (8.8.8.8, 1.1.1.1) — bypasses Docker daemon DNS.

### Review Workflow Notes

Android-18's security reviews catch issues that initial implementations miss:
- Build context path resolution edge cases
- Hardcoded external DNS leaks
- Missing healthchecks/resource limits

When 16 overwrites fixes (e.g., `0178c018` → `75cabc4e`), re-apply with clearer commit messages to prevent future overwrites.

---

## Session: AMD Compose File Review (Android-17)

**Time:** ~10:30 EDT
**Trigger:** Android-16's message about adding security/resource config to `docker-compose.amd.yml`

### Problem Identified
Android-16 over-added configuration to the AMD overlay file. The `docker-compose.amd.yml` is designed as a **minimal overlay** that layers on top of `docker-compose.base.yml` — it should only specify AMD-specific overrides.

**What 16 added (37 lines of duplication):**
- `security_opt: no-new-privileges:true` — already in base
- Full `environment:` block — already in base
- `ports:` — already in base
- `deploy.resources.limits/reservations` — already in base
- `healthcheck:` — already in base
- `dns:` — already in base

### Fix Applied (Commit `9a5d34c7`)
Removed all duplicated configuration from `dashboard-api` service in the AMD overlay. The file now correctly contains only:
- `extra_hosts: host.docker.internal:host-gateway` — AMD-specific networking
- `volumes: /sys/class/drm:/sys/class/drm:ro` — AMD GPU monitoring

### Pattern Reinforced
**Overlay files should only specify what's different from base.** Duplicating configs creates maintenance burden — when base changes, overlays become stale.

### Whisper TTL Addition (Commit `7e7d54b1`)
Also committed pending change: added `WHISPER__TTL=86400` environment variable to whisper NVIDIA compose for 24-hour cache TTL control.

## Next Session Priorities (per workstream order)
1. GitHub issues (remaining open issues: #32, #33, #22)
2. Hardening wave
3. Extensions
4. Installer testing
5. Upstream monitoring

---

## Session: Pre-Compaction Memory Flush

**Time:** ~10:30 EDT
**Purpose:** Capture durable memories before session compaction

### Key Learnings for MEMORY.md

**Docker Compose Build Context Resolution:**
When running `docker compose -f extensions/services/X/compose.yaml` from repo root:
- `context: .` resolves relative to compose file location (service directory) → **CORRECT**
- `context: extensions/services/X` doubles the path → **WRONG**

**DNS Configuration:**
Use only Docker's internal DNS (`127.0.0.11`) to avoid DNS leaks and external dependencies.

**Overlay Files Pattern:**
AMD/NVIDIA overlays should only specify what's different from base — never duplicate configs.

**Commit Review Workflow:**
16 often overwrites fixes (e.g., `0178c018` → `75cabc4e`). Always verify current state and re-apply with clear commit messages to prevent future overwrites.

---

## Session: 18's Review Queue Verification (Android-17)

**Time:** ~11:05 EDT  
**Trigger:** Queued Discord messages from Android-18 reviewing commits `3737a17b` through `5ac03509`

### Review Findings — All Issues Resolved

| Commit | Issue | Status | Fix Commit |
|--------|-------|--------|------------|
| `3737a17b` | Missing "Respond to Webhook" node (webhook hang) | ✅ Fixed | `5220dc2b` |
| `3737a17b` | HTTP Request nodes lack timeout | ✅ Fixed | `a0bd8f2a` |
| `9c4bf1e2` / `362afed9` | AMD overlay security regression | ✅ Not a bug | Overlay pattern correct |
| `1359de0a` | open-interpreter missing security patterns | ✅ Fixed | Current file has all patterns |
| `f2951ed4` | Healthcheck uses curl (not in image) | ✅ Verified | Uses `wget` |
| `5ac03509` | Timing side-channel in safeCompare | ✅ Fixed | `b463b719` / `fc41572b` |

### Key Fixes Applied by 16

**1. safeCompare Timing Attack Fix (`b463b719`, `fc41572b`)**
```javascript
// Fixed: Pad to fixed length (128 chars) to prevent length oracle attacks
function safeCompare(a, b) {
  const MAX_LEN = 128;
  const padA = a + '\0'.repeat(Math.max(0, MAX_LEN - a.length));
  const padB = b + '\0'.repeat(Math.max(0, MAX_LEN - b.length));
  
  if (a.length > MAX_KEY_LENGTH || b.length > MAX_KEY_LENGTH) return false;
  
  let result = 0;
  for (let i = 0; i < MAX_LEN; i++) {
    result |= padA.charCodeAt(i) ^ padB.charCodeAt(i);
  }
  return result === 0;
}
```
Pattern: Fixed-length comparison (always 128 iterations) + early length rejection prevents timing attacks.

**2. n8n Webhook Response Fix (`5220dc2b`, `83c83432`)**
- Added missing "Respond to Webhook" node to both `webhook-trigger.json` templates
- Removed redundant `webhookSuffix: ""` configuration
- HTTP timeouts added (`timeout: 30` / `timeout: 120000`)

**3. AMD Overlay Clarification**
The `docker-compose.amd.yml` correctly contains only the AMD-specific volume mount:
```yaml
dashboard-api:
  volumes:
    - /sys/class/drm:/sys/class/drm:ro
```
All security patterns (`security_opt`, `healthcheck`, `deploy.resources.limits`) are inherited from `docker-compose.base.yml`. This is the correct overlay pattern.

**4. open-interpreter Security Patterns**
Current `compose.yaml` has all required patterns:
- ✅ `security_opt: [no-new-privileges:true]`
- ✅ `healthcheck` (uses `wget`, not `curl`)
- ✅ `deploy.resources.limits` (2 CPU, 4GB RAM)

### Status
**Working tree clean** — all 18's flagged issues resolved in commits subsequent to the reviewed ones.

---

## Session: Commit Review `01d6977b` (11:13 EDT)

**Trigger:** Android-18 queued commit review

### Commit `01d6977b` — Memory Log Update
**Author:** Lightheartdevs  
**Type:** Non-code file update (documentation)  
**Change:** Log full QA review session (7 commits approved, security fixes verified)

**Status:** ✅ **Approved** — Documentation update only, no code changes
**QA Verification:** All 7 commits previously reviewed and approved:
- `b463b719` — n8n scheduled-task timeout
- `e70cb808` — whisper sed/perl escaping
- `5ac03509` — flowise/langflow security fixes
- `e2530e6b` — n8n template fixes
- `5220dc2b` — webhook response node
- `a0bd8f2a` — HTTP timeout additions
- `fc41572b` — timing-safe comparison

**Security Fixes Verified:**
- ✅ timingSafeEqual pattern: fixed-length 128-char comparison with early length rejection
- ✅ DoS prevention: iterations only to actual string length
- ✅ Auth bypass removed: no early return on length mismatch
- ✅ SSL bypass removed: uses HTTP for internal container networking
- ✅ Error disclosure: generic error messages only

---

## Session: Commit Review `90b6d2a4` (11:15 EDT)

**Trigger:** Android-18 queued commit review

### Commit `90b6d2a4` — HTTP Timeout Fix
**Author:** Lightheartdevs  
**Type:** Bug fix  
**Change:** Correct HTTP timeout from `30` (0.03s) to `30000` (30s)

**Review by K2.5:**
- ✅ **Unit mismatch fixed:** n8n HTTP Request nodes expect timeout in milliseconds
- ✅ **Correct value:** `30000` = 30 seconds (was `30` = 0.03 seconds)
- ✅ **Expression syntax:** Both files use correct `={{ }}` syntax (not Jinja `{{ }}`)

**Impact:** Webhooks will now properly wait up to 30 seconds for external API responses instead of timing out after 30ms.

### Next Session Priorities (per workstream order)
1. GitHub issues — #32 (Windows), #33 (deployment verify)
2. Hardening wave — remaining checklist items
3. Extensions — Wave 6 preparation (Tailscale, AdGuard)
4. Installer testing — coordinate with Bilal
5. Upstream monitoring — verify no rot in deployed extensions
