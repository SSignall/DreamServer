# 2026-03-08 — Android-17 Daily Notes

## Multi-GPU Detection Fix (GitHub Issue #55)
- Reviewed GitHub Issue #55: Dual GPU detection not working (RTX 3090 + 4090)
- Root cause: `detect_nvidia()` in `detect-hardware.sh` was using `| head -1` which only returns first GPU
- Fix applied: Removed `| head -1` from `detect_nvidia()`, added `detect_nvidia_first()` helper for single-GPU contexts
- Verified multi-GPU VRAM summing already working in installer (`detection.sh` uses `awk` to sum)
- Committed & pushed: `f3a07946`

## asyncio.get_event_loop() Deprecation Fix
- Android-18 flagged `asyncio.get_event_loop()` as deprecated in Python 3.10+
- Replaced with `asyncio.get_running_loop()` in `helpers.py` (2 locations)
- Committed & pushed: `f3a07946`

## RVC Workflow Path Fix Verification
- Reviewed commit `89f31dd4`: Restored leading slashes in RVC workflow paths (`/upload`, `/convert`)
- Base URL now strips trailing slash: `{{ ($env.RVC_HOST || 'http://rvc:7865').replace(/\/$/, '') }}`
- Path has leading slash: `"/upload"` and `"/convert"`
- Final URL: `http://rvc:7865/upload` (correct)
- Commit `b5abebdd` was reverted by `89f31dd4` — correct behavior restored

## Service Registry Security Fixes (Commit 48275914)
- Added `_safe_cd()` helper to validate paths before navigation
- Path traversal protection: rejects `..` and `//` in paths
- Added `|| exit 1` after `_safe_cd()` calls to fail fast on invalid directories
- EXTENSIONS_DIR validation added at script end

## Commit Review Batch (Android-18)
| Commit | Status | Notes |
|--------|--------|-------|
| `b5abebdd` | Reverted | Removed leading slashes — broke n8n URL construction |
| `89f31dd4` | ✅ Fixed | Restored leading slashes with proper URL cleanup |
| `4e273d5c` | ✅ Approved | Case-insensitive URL encoding validation |
| `3b71514d` | ✅ Approved | Memory updates |
| `a30eb2bc` | ✅ Approved | Memory updates |

## Git Activity (Today)
- `f3a07946` — fix: replace deprecated asyncio.get_event_loop() with get_running_loop()
- `2860cf86` — fix(rvc): remove redundant backslash check in model path validation
- `e866df5f` — docs(memory): add Android-18 commit review findings for recent batches

---

## Wave 3 Extension QA Review (Earlier in Session)
- Reviewed 16's Wave 3 batch (commits dfe4fea8 through 0f207a9b)
- Found and fixed piper-audio ID mismatch (commit 2ecc3e84)
- Added schema support for empty health field (CLI tools)
- All 405 extension checks pass (0 failed, 1 warning for port conflicts)
- Pushed to dev/main: 45444207

## Hardening Wave 3 Status
- Wave 3 extensions validated and committed
- XTTS pinned to v1.0-cuda121, Milvus simplified to standalone mode
- Aider healthcheck removed (CLI tool), schema allows empty health

## Healthcheck Validation Script Fix
- Validation script now correctly skips healthchecks for CLI tools
- Verified `aider`: "Healthcheck skipped (CLI tool)" ✅
- Verified `piper-audio`: "Healthchecks present" ✅
- Committed & pushed: `b916a617`

## RVC Model Parameter Security Regression
- Commit 1a2051cd (binary+parameters refactor) accidentally stripped path traversal protection
- Commit 89959a15 had added validation: checks for `..`, `/`, `\`, `%2e`, `%2f`, `%5c`, allowlist `^[a-zA-Z0-9._-]+$`, max 100 chars
- Fix 7e5bea48 restored validation to `parameters.model` while keeping binary+parameters structure
- Validation now active: blocks path traversal attacks, allows only safe filenames

## Android-18 Commit Review Follow-up (Sat 2026-03-08)

### Commit a75a5bb4 — Privacy Shield test_server.py (CRITICAL/HIGH)
- **Critical:** Fixed `global _models_loaded` binding bug — changed from `global _models_loaded` with `from ... import` to `server._models_loaded` via module import
- **High:** Service ID correctly set to `privacy_shield` (snake_case) to match directory name
- **Medium:** Test imports now use `from extensions.services.bark import server` for proper module reference when running from `research/generated-tests/`
- **Medium:** Restored `| head -1` in `detect-hardware.sh` for multi-GPU compatibility; caller uses `wc -l` to count GPUs
- **Low:** Completed truncated test (`test_tts_stream_generation_error`), added `test_load_models_thread_safety`, added module-level `import numpy as np`
- **Low:** Refactored to use module reference instead of direct underscore-globals import
- **Status:** Committed `3732fd3e`, pushed to dev/main

### Commit 04374d0a — Multi-GPU Installer Detection (MEDIUM)
- **MEDIUM:** Removed duplicate `GPU_COUNT` assignment (was on lines 95 and 100)
- **MEDIUM:** Fixed fragile PCI ID extraction — reverted from `xargs` + `${pci_ids:0:6}` to `head -1 | xargs` for safer single-GPU ID extraction
- **Status:** Committed `56b74d6e`, pushed to dev/main

### Commit e5dbdf8e — RVC Workflow & text-gen-webui Healthcheck (HIGH/MEDIUM)
- **HIGH:** RVC Discord node syntax fixed — removed backticks, wrapped entire expression with `={{ }}`
- **MEDIUM:** RVC HTTP nodes (id 2, 3) now include `body` parameters for file upload and model parameters
- **MEDIUM:** text-generation-webui healthcheck reverted from `CMD-SHELL` to `CMD` array (secure, no shell invocation)
- **LOW:** RVC workflow uses standard `$json` instead of `$item().json` for previous node output
- **Status:** Committed as part of `4e273d5c`, pushed to dev/main

### Commit 90ffc990 — text-generation-webui API Port (Already Reviewed)
- **Status:** Previously committed; environment variable `${TEXT_GEN_WEBUI_API_PORT:-5001}` used instead of hardcoded 5001
