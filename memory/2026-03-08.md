# Memory - 2026-03-08

## Session Overview
- Agent: Android-16 (Product Engineer, Local GPU)
- Role: Dream Server extension builder, CI/QA pipeline monitoring, GitHub issue triage
- Mission: Build extensions, fix bugs, harden stack, test installer, build workflow templates

## Ops Ping Response (19:43 EST)
Android-18 pinged: tag-teaming with Todd. Todd building Wave 2 extensions, I'm handling hardening.

## Today's Progress

### Hardening: Pin Container Image Tags
Updated services with floating tags to pinned versions:

| Service | Old Tag | New Tag | Commit |
|---------|---------|---------|--------|
| fooocus | `:edge` | `v2.5.5` | - |
| jupyter | `:latest` | `python-3.11` | - |
| openclaw | `:latest` | `v2026.3.2` | - |
| searxng | `:latest` | `2026.3.6` | - |
| perplexica | `:slim-latest` | `@sha256:7e11...` | - |
| whisper (speaches) | `:latest-cpu` | `v0.2.0` | - |

### CI/QA
- `make lint` — shell syntax warning exists for heredoc in command substitution (false positive — script runs fine)
- `make test` — 36 tier tests passed, installer contracts passed
- CI pipeline: validate-compose.yml runs on push

### Workstream Status

**Wave 1 (10/10 complete):**
- Ollama ✅, SillyTavern ✅, Dify ✅, Fooocus ✅, ChromaDB ✅, Piper ✅, Aider ✅, RVC ✅, Jupyter ✅, Immich ✅

**Wave 2 (6/10 complete):**
- #11 LibreChat ✅ (`823ad213`) — Todd
- #12 AnythingLLM ✅ (`22a76b15`) — Todd
- #13 Flowise ✅ (`7e1e4fa0`) — Todd
- #14 Langflow ✅ (`cd7cba99`) — Todd
- #15 Open Interpreter ✅ (`79603b24`) — me
- #16-20 in queue

### Technical Notes
- Service registry uses embedded Python to parse all manifests at runtime
- CLI validates extension by checking if `$ext_dir` exists (`[[ -d "$ext_dir" ]]`)
- Extension structure: `extensions/services/<name>/` with `manifest.yaml`, `compose.yaml`, `README.md`
- Git remotes: `origin` (public), `dev` (push here for agent branches)
- Port conflict fix: Langflow uses port 3004 (external) → 7860 (internal) to avoid OpenCode collision
- Open Interpreter: port 3005 (external) → 8000 (internal)

## Push Status
**Committed and pushed:**
- `f200e1e2` — hardening: pin container image tags to stable versions
- `262497e1` — docs: update MEMORY.md with hardening work
- `d177ab71` — docs: update daily notes with hardening work
- `5bf22400` — docs: update MEMORY.md with Wave 2 progress and remaining hardening items
- `74d7d0da` — feat: add Open Interpreter extension for Wave 2 (#15)
- `79603b24` — extensions: add Open Interpreter (#15) + pin Langflow version

**Branch:** main → dev repo (`SSignall/Lighthouse-AI-Dev`)

## Blocked
None — working freely on extensions.

## Next Steps (Priority Order)
1. Remaining Wave 2 extensions (#16-20) — me
2. Hardening items — ComfyUI git pin, seccomp removal, SearXNG secret key
3. Run full CI on dream-server/ directory
4. Test install flow on .143

---

## Todd's Update (20:02 EST)

**GitHub Status:**
- No open issues on main repo
- Wave 2: 4/10 complete at time of report
- Langflow (#14) shipped — commit `cd7cba99`

**Langflow Details:**
- Image: `langflowai/langflow:1.8.0` (pinned)
- Port: 7860 external
- Features: Visual workflow builder, RAG pipelines, AI agents, 50+ integrations
- Healthcheck: `/health` endpoint
- Security: `no-new-privileges`, resource limits

**Open Interpreter Details:**
- New Wave 2 extension for local code execution via LLMs
- Python-based FastAPI server for OpenAI-compatible API
- Port 8000, health endpoint /health, no GPU required
- Validation: 266 checks passed, committed `79603b24`

**Installer Test Results:**
- Extension discovery validated (20 services)
- `--non-interactive` and `--core-only` flags working
- Model download bottleneck: ~60 min for 4GB on .143
- `--core-only` committed: `de14e3db`

**Current Split:**
- **Me (Android-16):** Extensions (#16-20 Wave 2), upstream monitoring, remaining hardening
- **Todd:** Extensions (#16-20), hardening items

**Hardening Remaining:**
- Generate SearXNG secret key at install time
- Tighten OpenClaw auth defaults
- Remove API key embedding from token-spy
- Pin ComfyUI git clone to specific commit/tag
- Remove `seccomp:unconfined` and `SYS_PTRACE` from ComfyUI AMD compose

---

## Installer Test Session (20:00 EST)

**Participants:** Android-16, Android-17

**Findings:**
- ✅ Extension discovery (20 services)
- ✅ `--non-interactive` flag working
- ✅ `--core-only` flag added and committed (`de14e3db`)
- ✅ `--all` flag working
- ✅ `--tier N` flag working

**Bottleneck:** Model download ~60 min for 4GB on .143 (slow network suspected)

**Next Steps:** Either wait for download completion or pivot to building extensions (Open Interpreter started)

---

## Android-17 Session Update (20:52 EST)

**Task:** QA review of 16's Wave 3 extensions

**Findings:**
- Wave 3 batch 1: 17 extensions (commit `23d5af2e`)
- Wave 3 batch 2: 1 extension (milvus, commit `c71d428e`)
- 16 was committing to `dream-server/extensions/` instead of root `extensions/`
- 16 already moved extensions to correct location (commit `1fc9f2cc`)

**QA Issues Found & Fixed:**
1. `milvus` — only had `manifest.yaml`, missing `compose.yaml` (16 fixed)
2. `xtts` — used `:latest-cuda121` tag, not pinned (16 fixed)
3. `rvc` — uses random Docker Hub user `aladdin1234/rvc-webui` (security flag noted)
4. `aider` — schema validation failed: `health: ""` (empty string) and piper-audio ID mismatch

**Fixes Applied:**
1. `piper-audio` — Changed service ID from `piper-tts` to `piper-audio` to match directory
2. Updated container_name, default_host, feature references to `piper-audio`
3. Updated schema to allow empty health string (`minLength: 0`) for CLI tools
4. Committed `2ecc3e84`

**Validation Results:**
- 405 passed, 0 failed, 1 warning (port conflicts), 5 skipped
- All extensions now pass validation

**Review of Recent Commits (21:00 EST):**
- `0f207a9b` — memory/2026-03-08: add Android-17 QA review of Wave 3 ✅
- `2ecc3e84` — fix: piper-audio ID mismatch, schema allow empty health ✅
- `1b1bcb2a` — fix(validation): allow empty health field for CLI tools ✅
- `dcee96c5` — fix: piper-audio ID consistency, milvus etcd endpoint protocol ✅
- `e1d8254f` — fix: hardening issues (SYS_ADMIN, JWT_SECRET, pinXTTS, limits) ✅
- `a5f20740` — fix(extensions): validation fixes for aider, ollama, piper, qdrant, xtts ✅
- `1561d8bd` — fix(structure): remove duplicate extensions from dream-server/ ✅
- `7aa3f03b` — fix: address review findings from 1fc9f2cc ✅
- `be4c4809` — fix(extensions): QA fixes for Wave 3 ✅
- `dfe4fea8` — fix(invokeai): use v5.5.0 (v6.11.1 doesn't exist) ✅

**All commits validated. Ready for next task.**

## Open Interpreter (#15) — Shipped (20:04 EST)

**Commit:** `79603b24`

**Details:**
- New Wave 2 extension for local code execution via LLMs
- Python-based FastAPI server for OpenAI-compatible API
- Features: code execution, chat, browser control, data analysis
- Port 8000, health endpoint /health, no GPU required
- Compose builds from Dockerfile (Python 3.11-slim)
- Pinned to open-interpreter package version
- Validation: 266 checks passed

**Wave 2 Status:** 6/10 complete

Next up: #16-20 Wave 2 extensions. Ready for hardening items or continue building.
