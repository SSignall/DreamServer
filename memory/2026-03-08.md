# 2026-03-08 — Android-17 Session Log

## Work Completed

### 1. Docker Compose Linux Compatibility Fix
**Commit:** `c3e60924`
- Fixed `host.docker.internal` issue in `docker-compose.base.yml`
- Changed from hardcoded value to `${SERVICE_HOST:-host.docker.internal}`
- Allows Linux Docker CE users to override (e.g., `SERVICE_HOST=172.17.0.1`)
- Docker Desktop users continue working with defaults
- **Impact:** Prevents connectivity failures on standard Linux deployments

### 2. RVC Security Hardening — Path Traversal Fix
**Commit:** `b4ace613`
- Added `Validate Input` node to RVC workflow (`workflow-voice-convert.json`)
- Path traversal detection: blocks `../`, `..\`, `%2e%2e` (URL-encoded), `\x5c` (hex-escaped)
- Model name validation: alphanumeric, dash, underscore only
- Returns 400 error on validation failure
- Updated RVC Convert node to use validated inputs via `$('Validate Input')`
- **Security impact:** Prevents directory traversal attacks via model/audio_path parameters

### 3. QA Review Activity
- Reviewed 16's n8n Wave 3 commits (seed validation, JSON syntax fixes)
- Monitored commit review stream from Android-18
- Coordinated with 16 on extension work split

## Context
- Supervisor: Android-18 (Michael away)
- Workstream priority: Extensions → Hardening → Upstream monitoring
- No open GitHub issues requiring immediate attention
- 16 focused on ComfyUI workflows; 17 took hardening items

## Technical Notes
- Both fixes are Tier 0 autonomy (bug fixes, no architecture changes)
- All work pushed to `dev/main` on Lighthouse-AI-Dev repo
- JSON validation: Both commits tested for valid JSON/YAML
