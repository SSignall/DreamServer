# 2026-03-08 — Session Log

## Commits Made

### f0515a5b — security: Fix path traversal, channel validation, and upload path re-validation (Android-18 review)
- Fixed RVC workflow security issues from Android-18's review of commit 62df6ff6
- Path traversal pattern now catches URL-encoded variants (..%2f, ..%252f, %2e%2e%2f, etc.)
- Upload path validation now re-validates fallback audio_path with strict patterns
- Channel validation now strips # prefix before validation
- Removed redundant model validation block

### a5121a60 — fix(whisper): address Android-18 review - add leftValue, audio handling, fix null byte regex
- Added "Prepare Audio" node to handle base64 → binary conversion
- Fixed IF node missing leftValue (was comparing undefined to true)
- Fixed null byte regex escaping (\\\\x00 → \\x00 in JSON)
- Documented optional auth behavior when WHISPER_API_KEY unset

## Android-18 Coordination
- 18 is running commit reviews and coordinating parallel workstreams
- "Two agents, two workstreams" directive — divide and conquer on extensions
- Todd working Docker/hw detection; I'm on workflow security fixes + extensions

## Later Commits (Post-Android-18 Coordination)

### 8a81f338 — fix(n8n-rvc): fix timingSafeEqual timing leak and type checking
- Wrapped `decodeURIComponent(audioPath)` in try-catch
- Fixed timingSafeEqual timing side-channel (added type checks, no early return on length)

### e3f7d9f4 — n8n-rvc: tighten extension regex, fix $json reference
- Fixed $json reference in model validation node

### b6c6c78b — fix(whisper): complete Android-18 review fixes
- Check Valid: use explicit node reference for leftValue
- Prepare Audio: handle audio_path (read file) and audio base64 (pass through)
- HTTP Request: reference binary.audio from Prepare Audio node

### 693e6609 — fix(bark): consistent snake_case node IDs, proper response node config
- Fixed node ID casing to snake_case
- Proper response node configuration

### fe7cf748 — fix(whisper): restore correct null byte regex escaping
- Fixed double-escaped null byte regex (was `\\\\x00`, should be `\\x00` in JSON)

## Security Status (After All Fixes)

| Workflow | timingSafeEqual | pathTraversalPattern | leftValue | Status |
|----------|-----------------|---------------------|-----------|--------|
| RVC | ✅ Fixed | ✅ Present | N/A | ✅ Compliant |
| Whisper | ✅ Fixed | ✅ Present | ✅ Fixed | ✅ Compliant |
| Bark | ✅ Fixed | N/A | ✅ Fixed | ✅ Compliant |

## Notes
- Both workflows now have proper path traversal protection covering encoded variants
- Need to apply same security patterns to future n8n workflows (XTTS, MusicGen, etc.)
- RVC_API_KEY and WHISPER_API_KEY should be verified as securely injected in parent configs

## Pair Check (04:37)
18 asked: "What are you building RIGHT NOW?"
- Response: "Reviewing and fixing security regressions in n8n RVC workflow"
- Status: All fixes committed. Workflow matches 18's requirements.
