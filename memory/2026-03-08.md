# Memory Log — 2026-03-08

## n8n Wave 3 QA Review — Complete

**Trigger:** Android-18 review request for 16's commits (e2967cbd through 9eb284d7)

### Commits Reviewed
| Commit | Description |
|--------|-------------|
| `e2967cbd` | Memory update |
| `dfa5ea5a` | ComfyUI fixes (superseded) |
| `8695c69f` | ComfyUI issues (superseded) |
| `b09f5e06` | Installer test |
| `9eb284d7` | ComfyUI base workflow |

### Android-18 Review Findings (K2.5)
| Severity | Issue | Location |
|----------|-------|----------|
| HIGH | Discord Error node references `$('Validate Input')` — undefined when validation fails | workflow-n8n-comfyui.json:78 |
| MEDIUM | Seed logic treats 0 as falsy: `parseInt(input.seed) || random` replaces valid seed 0 | workflow-n8n-comfyui.json:19-20 |
| LOW | `parseInt()` without radix may parse octal on legacy engines | workflow-n8n-comfyui.json:19 |

### Fixes Applied (Commit `eb01f5a4`)
1. **Error handler channel:** Changed from `$('Validate Input')` to `$('Webhook Trigger')` — ensures error notifications work even when validation fails
2. **Seed handling:** Changed from `parseInt(input.seed) || random` to `input.seed != null ? parseInt(input.seed, 10) : random` — preserves seed=0 as valid
3. **parseInt radix:** All `parseInt()` calls now include radix 10 for safety

### Review Queue Status
✅ Clear — all quality issues fixed and committed to dev/main

## Hardening Wave Status

**Recently Completed:**
- JSON escaping: Using `python3 json.dumps()` for RFC 8259 compliant token generation
- RVC path validation: Restored literal backslash check for path traversal protection
- LocalAI extension: Fixed `external_port_env` mismatch (LOCALAI_PORT → LOCALAI_EXTERNAL_PORT)

**Remaining (from March 6 Audit):**
| ID | Severity | Description | Effort |
|----|----------|-------------|--------|
| S-10 | HIGH | Timing side-channel in password comparison | 15 min |
| S-27 | HIGH | Redis has NO authentication (requirepass) | 30 min |
| S-14 | HIGH | CSP allows 'unsafe-inline' and 'unsafe-eval' | 30 min |
| S-9 | HIGH | Refresh token NOT rotated on use | 1 hr |
| S-8 | HIGH | HS256 symmetric HMAC instead of RS256 | 2 hrs |
| S-28/29 | HIGH | No read_only flags on volume mounts | 15 min |

## Workstream Priority (Confirmed)
1. GitHub issues from real users
2. Security fixes (hardening wave)
3. Extensions (Flowise, Dify)
4. Workflow templates for completed extensions
5. Installer testing
6. Upstream monitoring
7. Content & community
8. Quality/hardening background work

## Git Workflow
- All agent work pushes to `dev/main` on Lighthouse-AI-Dev repo
- NEVER push to public origin (pre-push hook enforces)
- Commit messages: `fix(n8n): ...`, `feat(extension): ...`, `docs(memory): ...`
