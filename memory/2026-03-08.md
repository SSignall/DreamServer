# 2026-03-08 — Security Fixes & Installer Testing Coordination

## Session Summary

**Pushed commits to dev/main:**
- `cbd3d0e8` — fix(whisper): remove variable expansion in docker-entrypoint.sh
- `0c06366b` — fix(whisper): hardcode DREAM_PATCHED marker
- `159dfdf1` — fix(whisper): hardcode DREAM_PATCHED marker (revert of 0c06366b)
- `da5c22cd` — fix(langflow): eliminate timing side-channel in safeCompare
- `30cde01c` — memory/2026-03-08: log dev/main push
- `a669a7b4` — docs: add security hardening checklist

**Security Fixes Verified:**
- ✅ Whisper entrypoint — Uses hardcoded perl/sed patterns, no user input in commands
- ✅ Langflow `safeCompare` — Removed `Math.max(a.length, b.length)` which allowed length oracle attack
- ✅ Security pattern flagged as regression — False positive (style cleanup, security intact)

**Review Queue Status:** ✅ CLEAR

**GitHub Issues:**
- #55 — Dual GPU detection — Already fixed (`f1e10480`), needs Michael to close
- #32 — Windows installer — Missing `install.ps1`, unassigned
- #33 — Dashboard offline — Fix `8ea4bd2e` committed, deployment verify pending

**Security Hardening:**
- Wave 4 — ✅ Complete (SearXNG key, OpenClaw binding, image pinning, ComfyUI AMD opts)
- Wave 5 — ⏳ Pending (RVC, Bark, Ollama, LocalAI)

**Next Priority:** Installer testing with Bilal

## CI Verification
Blocked on GitHub CLI auth. Michael to run `gh run list --limit 5` when available.

## Handoff to Bilal
Sent installer testing matrix via Discord channel:
- #55 dual GPU detection (test multi-GPU systems)
- #32 Windows installer (missing `install.ps1`)
- #33 dashboard service health (verify `8ea4bd2e` deployed)
- Test matrix: Ubuntu/Fedora/Debian/Arch (NVIDIA/AMD), Windows (NVIDIA/AMD), macOS Apple Silicon

## Security Hardening Checklist
Created `docs/security-hardening.md` — centralized tracking for team.
