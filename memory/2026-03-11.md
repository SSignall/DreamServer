# 2026-03-11 — Pre-Compaction Memory Flush

## Recent Work Summary (Current Session)

### Session Focus
- GitHub issue triage and review queue clearing
- Hardening wave progress (JSON escaping, RVC, LocalAI fixes)
- Extension workflow development
- RVC pitch validation improvement

### Key Commits Reviewed (Last Batch)
- `952a59b5` — Restored literal backslash check in RVC path validation (HIGH from 18)
- `fd7c65c3` — Added `OPENCLAW_TOKEN_JSON` with Python3 `json.dumps` escaping
- `84021dde` — macOS installer: Python3 `json.dumps` for robust token escaping
- `97540fed` — LocalAI extension: fixed `external_port_env` mismatch
- `48418c13` — Memory update commit

### New Commits (This Session)
- `5bd5d177` — fix(n8n-rvc): improve pitch validation with isNaN check and clearer error message
- `4aee2d35` — memory/2026-03-11.md — Update with RVC pitch validation fix
- `80351b37` — memory/2026-03-11.md — Update with Bark regression fix status
- `0f5417de` — docs(memory): log whisper workflow fixes

### Commits Fixed (Since Last Flush)
- `09fe6309` — fix(bark): restore IF node leftValue, fix connection IDs, responseMode (supersedes a7965b91)
- `b749ae09` — fix(n8n-rvc): add notes field, restore secure RVC Convert validation (supersedes 55936ccb)
- `b9f60ce6` — fix(n8n/rvc): security fixes for timingSafeEqual and decodeURIComponent (supersedes 9f74223d)
- `0f5417de` — fix(whisper): complete Android-18 review fixes (CRITICAL audio file attachment, leftValue, binary handling)
- `9f74223d` — fix(n8n-rvc): add decodeURIComponent try-catch, fix syntax error (superseded)
- `55936ccb` — fix(rvc): wrap decodeURIComponent in try-catch (superseded)

### GitHub Issues Status
| Issue | Status | Owner |
|-------|--------|-------|
| #33 (Dashboard offline) | In progress | Fix committed `8ea4bd2e`, pending deployment verification |
| #55 (Dual GPU detection) | NEW | Awaiting assignment |
| #32 (Windows install) | Unassigned | Pending assignment |
| #22 (OpenClaw gateway security) | Not a bug | Gateway binding to `127.0.0.1` is intentional |

## Hardening Wave Progress

### Completed Fixes
- ✅ JSON escaping: Python3 `json.dumps()` pattern for RFC 8259 compliance
- ✅ RVC path traversal: Literal backslash check restored alongside URL-encoded `%5c`
- ✅ RVC path traversal: Enhanced with URL-encoded slash patterns (`%2f`, `%252f`)
- ✅ RVC timing-safe fix: Constant-time comparison without early return on length mismatch
- ✅ RVC decodeURIComponent: Added try-catch for malformed URL encoding
- ✅ RVC pitch validation: Added isNaN check and improved error message
- ✅ LocalAI extension: `external_port_env` mismatch corrected
- ✅ LocalAI healthcheck: Now only checks `/healthz` (removed root path fallback)

### Hardening Checklist (Remaining)
- [ ] Continue through remaining items in MEMORY.md hardening list

## Git Workflow (Dev Server)
- Push all work to `dev/main` branch only
- Use `git push dev main`
- NEVER push to origin, create PRs, or feature branches for public repo

## Dev/Test Server (.143)
- Tower 2 (192.168.0.143) is free for testing Dream Server changes
- No Guardian protection — sandbox environment
- Coordinate loosely with other agents

## Installer Testing Notes (.143)

### Bug #1: Installer Hangs on Existing Installation
**Severity:** MEDIUM  
**Component:** `dream-server/install-core.sh`

**Issue:** Installer detects existing installation, logs warning, then hangs indefinitely without prompting user.

**Expected:** Prompt user or support `--force` / `--non-interactive` flags.

**Suggested Fix:** Add env var support:
```bash
FORCE=1 bash install.sh      # Overwrite without prompt
NONINTERACTIVE=1 bash install.sh  # Error out on existing install
```

### Bug #2: No Non-Interactive Mode
**Severity:** MEDIUM  
**Component:** Phase 2 feature selection

**Issue:** Installer cannot run unattended. No environment variable to pre-select Full/Core/Custom mode.

**Expected:**
```bash
INSTALL_MODE=full bash install.sh
INSTALL_MODE=core bash install.sh
```

### Environment Tested
- OS: Ubuntu 24.04.3 LTS
- GPU: NVIDIA RTX PRO 6000 Blackwell (95GB VRAM)
- CPU: AMD Ryzen 9 7950X3D
- RAM: 124GB

## Upstream Monitoring

### Detected Issues
- Windows installer hangs on existing installation
- Missing non-interactive mode for CI/CD use cases

### Fix Strategy
1. Add `FORCE`, `NONINTERACTIVE`, `INSTALL_MODE` env var support
2. Document non-interactive mode for CI/CD
3. Add timeout to interactive prompts (30s default)

## Current State

### Review Queue
- Status: Clear — all quality issues fixed and committed
- Latest Android-18 reviews:
  - `a7965b91` (bark timing-safe) → superseded by `09fe6309`
  - `55936ccb` (RVC security) → superseded by `b749ae09`
  - `9f74223d` (RVC try-catch) → superseded by `b9f60ce6`
  - `63d6e9cf` (whisper STT) → superseded by `0f5417de`
- Next: Monitor GitHub issues for new user reports

### Workstream Status
- GitHub issues: In progress ( triage complete, fixes committed)
- Hardening wave: Progressing
- Extensions: In progress (LocalAI workflow draft)
- Installer testing: Bug identified, awaiting implementation

## Git Activity
- Pushed to dev/main: `9eb284d7` through `0f5417de`
- Latest commits: `0f5417de` (whisper fixes), `80351b37` (memory update)

## Commits Pushed (This Session)
- `5bd5d177` — fix(n8n-rvc): improve pitch validation with isNaN check and clearer error message
- `4aee2d35` — memory/2026-03-11.md — Update with RVC pitch validation fix
- `80351b37` — memory/2026-03-11.md — Update with Bark regression fix status
- `0f5417de` — docs(memory): log whisper workflow fixes

## Next Session Priorities (per workstream order)
1. GitHub issues — #32 (Windows), #22 (gateway), #55 (dual GPU), #33 (deployment verify)
2. Hardening wave — remaining checklist items
3. Extensions — complete LocalAI with n8n workflow
4. Installer testing — implement non-interactive mode fixes
5. Upstream monitoring — verify no rot in deployed extensions

## Memories Archived from Older Files

### From 2026-03-10
- JSON escaping fix pattern: Primary `python3 json.dumps()`, fallback `sed`
- LocalAI extension `external_port_env` corrected
- RVC security: Path traversal protection restored

### From 2026-03-08
- Gateway binding security fix: Changed `"localhost"` → `"loopback"` in config
- Whisper STT workflow shipped (`63d6e9cf`)
- 18 = supervisor when Michael away

### From 2026-03-11
- Bark timing-safe fix regression (`a7965b91`) superseded by `09fe6309` (leftValue, connection IDs fixed)
- RVC security regression (`55936ccb`) superseded by `b749ae09` (notes, validation restored)
- RVC try-catch regression (`9f74223d`) superseded by `b9f60ce6` (timing-safe, decodeURIComponent fix)
- RVC path traversal protection: Enhanced with URL-encoded slash patterns (`%2f`, `%252f`)
- Whisper STT workflow (`63d6e9cf`) superseded by `0f5417de` (leftValue, audio handling, binary fixes)

### From 2026-03-07
- Wave 4 hardening complete (435 passed, 0 failed)
- privacy_shield ID mismatch fixed
- text-gen-webui port env var fix
- RVC security fixes (backslash check, URL encoding)

### From 2026-01-19
- Frigate port mismatch (8971 → 5000)
- n8n workflow template fixes
- Bark server security hardening (v0.1.6)

### From 2025-06-26
- Windows installer env var persistence
- Installer NvidiaGPU bug (get_nvidia_gpu_info return value)
- JSON escaping via Python3
- LocalAI external_port_env fix
- RVC path traversal protection

---

*This file will be archived during next pre-compaction flush. Durable memories moved to MEMORY.md.*
