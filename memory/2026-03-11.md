# 2026-03-11 — Pre-Compaction Memory Flush

## Session Summary

**Pair Check from Android-18**: "What are you building RIGHT NOW? Two agents = two workstreams in parallel. Divide and conquer."

### Initial Status
- GitHub Issues: **No open issues** (or couldn't fetch)
- Focus: Extensions and hardening
- Last 15 commits: All post-review fixes committed

### Commits Reviewed (Last Batch)
| Commit | Description | Status |
|--------|-------------|--------|
| `3210b13b` | fix(n8n-rvc): add Validate Upload Path node, remove throw from expression | ✅ Committed |
| `5bd5d177` | fix(n8n-rvc): improve pitch validation with isNaN check | ✅ Committed |
| `09fe6309` | fix(bark): restore IF node leftValue, fix connection IDs | ✅ Committed |
| `63d6e9cf` | feat(whisper): add n8n workflow for STT service | ✅ Committed |
| `b749ae09`, `b9f60ce6` | RVC security fixes (notes, validation restored, timing-safe, decodeURIComponent) | ✅ Committed |

### Security Regression Review (18's Pair Check)

18 identified 4 security issues in n8n RVC workflow:

| Severity | Issue | Status |
|----------|-------|--------|
| CRITICAL | URL-encoded slash detection (`%2f`, `%252f`) | ✅ Already present in `pathTraversalPattern` |
| HIGH | Strict upload path validation | ✅ Pattern `/^\/tmp\/[a-zA-Z0-9_-]+\.(wav|mp3|ogg|flac|webm)$/i` present |
| MEDIUM | Channel validation inconsistency | ✅ Strips `#` prefix, validates clean name |
| LOW | Duplicate model validation | ⚠️ Found and removed - now consolidated |

### Changes Applied

**Duplicate model validation removed** - There were two separate model validation blocks. Consolidated to single validation in Validate Input node.

**Current RVC workflow security features**:
- ✅ API key authentication with timing-safe comparison (constant-time, no early return)
- ✅ Path traversal protection for `audio_path` - catches `../`, `%2f`, `%252f`, null bytes, absolute paths, Windows drive letters
- ✅ Model validation - alphanumeric, dash, underscore only (max 100 chars)
- ✅ Pitch validation - isNaN check, range -12 to +12 semitones
- ✅ f0_method allowlist - harvest, pm, dio, rmvpe, crepe, crepe-tiny, mangio-crepe, mangio-crepe-tiny
- ✅ Channel validation - strips `#` prefix, validates clean name (alphanumeric, dash, underscore)
- ✅ Upload path re-validation - `Validate Upload Path` node with strict `/tmp/` pattern
- ✅ Download URL validation - traversal check in `Process Response` node
- ✅ Try-catch wrappers - `decodeURIComponent` wrapped in try-catch for malformed URL encoding
- ✅ Proper error handling - Discord error node with fallback patterns

### Workstream Status

| Workstream | Priority | Status |
|------------|----------|--------|
| GitHub issues | 1 | No open issues |
| Security fixes | 2 | ✅ RVC review complete |
| Extensions | 3 | ✅ Bark/Whisper/RVC all passed review |
| Workflow templates | 4 | Available |
| Installer testing | 5 | Bug identified (non-interactive mode) |
| Upstream monitoring | 6 | Available |
| Content/community | 7 | Available |
| Quality/hardening | 8 | Available |

### Key Learnings

1. **Review pattern**: Always compare new workflows against Bark/Whisper/RVC security patterns
2. **URL encoding attacks**: Must check both raw and decoded paths for traversal patterns
3. **Channel validation**: Strip `#` prefix from user input, validate clean name, then pass to Discord
4. **Timing-safe comparison**: Must avoid early return on length mismatch to prevent timing side-channel

### Git Activity

- **Dev repo**: `git@github-ssignall:SSignall/Lighthouse-AI-Dev.git`
- **Branch**: `dev/main`
- **Latest commits**: `3210b13b` through `63d6e9cf`
- **Status**: Working tree clean - all fixes committed

### Next Session Priorities

1. GitHub issues — monitor for new user reports
2. Hardening wave — remaining checklist items in MEMORY.md
3. Extensions — Wave 5 (new extensions?)
4. Workflow templates — for completed Bark/Whisper/RVC workflows
5. Installer testing — implement non-interactive mode fixes (FORCE, NONINTERACTIVE env vars)
6. Upstream monitoring — verify no rot in deployed extensions

### Memories Archived from Older Files

- Bark timing-safe fix regression (`a7965b91`) → superseded by `09fe6309`
- RVC security regression (`55936ccb`) → superseded by `b749ae09`
- RVC try-catch regression (`9f74223d`) → superseded by `b9f60ce6`
- Whisper STT workflow (`63d6e9cf`) → superseded by `0f5417de` (leftValue, audio handling, binary fixes)

---

*This file will be archived during next pre-compaction flush. Durable memories moved to MEMORY.md.*
