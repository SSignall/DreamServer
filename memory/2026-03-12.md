# 2026-03-12 — Android-17 Pre-Compaction Memory Flush (Evening)

## QA Review Queue — Status: Clear ✅

**Commits Reviewed (Last 20 on `dev/main`):**
- `ace06225` - Standardized all 12 auth patterns to OR variant
- `27bb0117` - Removed duplicate auth return from bark workflow  
- `d50309f1` - Fixed authentication bypass (all workflows)
- `4739e74a` - Applied consistent auth pattern to 8 workflow files
- `9508788d` - Addressed review feedback for get_all_gpus()
- `a5ae4f4c` - Fixed multi-GPU detection
- `f67575ea` - Fixed fail-open API key validation in RVC
- `e6cdefa9` - Fixed timingSafeEqual/null byte detection in Whisper
- `e08cb5c5` - Fixed timingSafeEqual length oracle in LocalAI
- `5e117608` - Removed duplicate workflow files

**Security Fixes Applied:**
- ✅ Authentication bypass fixed across all 12 workflows
- ✅ Fail-open vulnerabilities fixed (RVC, Bark/TTS)
- ✅ timingSafeEqual side-channel patched across all n8n workflows
- ✅ Null byte detection restored in Whisper/RVC
- ✅ Duplicate files removed from service folders

**GPU Fix (Issue #55):**
- `get_all_gpus()` now iterates ALL AMD GPUs via `_find_all_amd_gpu_sysfs()`
- `get_gpu_info()` returns first GPU for backward compatibility
- Dual GPU (3090+4090) detection should now work correctly

---

## GitHub Issues Status

| Issue | Status | Owner |
|-------|--------|-------|
| #33 (Dashboard offline) | Fix committed (`8ea4bd2e`) | 17 |
| #55 (Dual GPU detection) | Fix committed (`a5ae4f4c`, `9508788d`) | 17 |
| #32 (Windows install) | Unassigned | — |
| #22 (Gateway security) | Not a bug (localhost binding intentional) | 17 |

---

## Extensions — Wave 5 Complete ✅

All Wave 5 extensions committed and pushed:
- Flowise workflow template (`workflow-n8n-flowise.json`)
- Langflow workflow template (`workflow-n8n-langflow.json`)
- Dify workflow template (`workflow-dify.json`)
- LocalAI workflow template (`workflow-text-generation.json`)

**QA Checklist Verified:**
- Pinned versions (no `:latest`)
- Valid syntax (n8n `={{ }}` not Jinja `{{ }}`)
- No stubs (all files functional)
- Follows patterns (compared to open-webui/searxng extensions)
- Security (`security_opt: no-new-privileges`, resource limits, no hardcoded secrets)
- Health checks (every compose service)
- No duplicates (env vars, files, commits)

---

## Git Workflow — Dev Repo

**Push all work directly to main on the `dev` remote:**
```bash
git push dev main
```

**NEVER push to origin, create PRs on the public repo, or create feature branches for the public repo.**
Only Michael promotes code to public. A pre-push hook blocks this.

**Before EVERY push:**
```bash
cd dream-server && make ci
```
This runs lint (ruff + shellcheck + eslint) + backend tests (pytest) + frontend build (vite).

**After EVERY push, verify CI passed:**
```bash
gh run list --limit 1
```

---

## Next Session Priorities (per workstream order)

1. GitHub issues — #32 (Windows), #33 (deployment verify)
2. Hardening wave — remaining checklist items
3. Extensions — Wave 6 preparation (Tailscale, AdGuard)
4. Installer testing — coordinate with Bilal
5. Upstream monitoring — verify no rot in deployed extensions

---

## Guardian-Protected Resources (Tower 1 - .122)

The T-1000 Guardian watchdog runs as a root systemd service on .122. It monitors critical files
and services every 60 seconds. If you modify a protected file, Guardian will detect it, restore
from backup, and restart your gateway.

**Protected files (immutable, auto-restored if changed):**
- `~/.openclaw/openclaw.json` — your gateway config (model settings pinned)
- `~/.openclaw/agents/main/agent/models.json` — model provider definitions
- `~/.config/systemd/user/openclaw-gateway.service` — your systemd service file
- `/home/michael/token-monitor/main.py`, `start.sh`, `db.py`, `session-manager.sh`
- `/opt/smart-proxy/proxy.py`
- `/home/michael/token-monitor/data/settings.json`

**If you need to test or experiment → use .143 (Tower 2).**

---

## Dev Server — Tower 2 (.143)

`192.168.0.143` is **Tower 2**. All agents run on **.122** (Tower 1).
Android-16's gateway is at .122:18791, yours is at .122:18789, Todd's is at .122:18790.

.143 is where you test Dream Server changes before promoting to production.

- **Free to use**: Test Dream Server fixes, validate installer, run the full stack
- **No Guardian protection** on .143 — it's a sandbox
- **Coordinate loosely**: If another agent is mid-test on .143, check before overwriting their work
