# 2026-03-12 — Pre-Compaction Memory Flush

## Security Fixes: timingSafeEqual Side-Channel Vulnerabilities

Android-18 review identified timing side-channels in multiple n8n workflows. All fixes committed to `dev/main`.

### Commits

| Commit | Description |
|--------|-------------|
| `7eee8461` | fix(rvc): SECURITY - fix timingSafeEqual side-channel vulnerabilities |
| `eab78401` | fix(bark): SECURITY - fix timingSafeEqual length comparison side-channel |
| `7fd2149c` | fix(n8n): SECURITY - fix timingSafeEqual in LocalAI and Ollama workflows |
| `c44ea259` | fix(n8n): remove dead code from timingSafeEqual fixes |
| `c92abadd` | memory: archive 2026-03-11 pre-compaction flush |

### The Vulnerability

**Issue**: `timingSafeEqual` implementations used `Math.max(a.length, b.length)` for comparison length, which leaks length difference via timing side-channel.

**Pattern fixed**:
```javascript
// BEFORE (vulnerable):
const len = Math.max(a.length, b.length);
for (let i = 0; i < len; i++) { ... }

// AFTER (secure):
if (a.length !== b.length) return false;
// Then constant-time comparison
```

### Affected Workflows Fixed

- ✅ RVC Convert (`rvc-convert.json`)
- ✅ Bark TTS (`bark-tts.json`)
- ✅ LocalAI (`localai-chat.json`)
- ✅ Ollama (`ollama-chat.json`)

### Key Learning

Never use `Math.max()` for timing-safe comparison length. Always:
1. Early return on length mismatch (leaks only that lengths differ, not which is longer)
2. OR pad both to fixed length then compare
3. First option acceptable for API key comparison — length doesn't reveal key content

---

*Pre-compaction flush complete. Durable memories archived.*
